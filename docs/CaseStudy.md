# Pepscore Lab — Case Study

**A back-office operations platform built through AI-directed engineering, not manual solo development.**

This case study is written for a professional portfolio and is grounded entirely in this repository's own record: `docs/Decisions.md` (60 logged engineering decisions), `docs/ChangeLog.md`, `docs/ComponentMap.md`, `docs/ProductRoadmap.md`, `docs/PaymentReadiness.md`, `docs/Architecture.md`, `docs/InvoiceSystemSpec.md`, the Prisma schema, and `git log` (164 merged PRs, chronological). Where the evidence is incomplete, ambiguous, or a claim can't be sourced, that is stated explicitly rather than filled in. No fabricated metrics, customer data, or accomplishments appear below. This document is maintained continuously — see "Continuous Update Rule" near the end.

---

## 1. Executive Summary

Pepscore Lab is a peptide-research-supplier e-commerce and back-office platform: a public storefront for research-use-only compounds, and — the larger and more mature half of the system — an internal operations engine that turns every sale (Stripe checkout *or* a manual/off-platform transaction — DM, cash, Cash App) into one trackable, invoiced, fulfillable record.

**The business problem it solves**: the owner was losing track of manual sales conducted outside the storefront (`docs/ProjectOverview.md`) — no consistent invoicing, no unified payment history, no real fulfillment record for a DM/cash sale the way a Stripe order automatically gets one.

**Product vision**: one coherent back office that scales "from one person taking manual orders over DM to a fully automated, multi-channel operation — without ever needing to throw away and rebuild what came before" (`docs/ProjectOverview.md`). The architecture decisions throughout this project are explicitly evaluated against that bar — extend a proven seam, don't build a parallel one.

**Target users**: the owner/operator (admin dashboard, invoicing, CRM, fulfillment) and, as of Phase 2A/2B, the customer directly (intake forms, a Customer Portal, and a public storefront).

**Scope actually built**: a full invoice system (dual PDF output, payment arrangements, archiving, trash/recovery), carrier-agnostic shipment tracking with real Shippo integration, a centralized Fulfillment Gate, a CRM layer (Customer, activity logs, lead capture), a Customer Identity Platform (portal accounts, claim flow, rollout automation), a payment-provider abstraction (Stripe today, ACH, saved payment methods), inventory/pricing (case/SPA/individual-vial sell units, backorders), a full price-change audit trail with pre-commit preview, a sell-unit-aware reorder system (Customer Portal Buy Again/Reorder All, admin-assisted reorder from a customer's purchase history), a centralized branded email design system with an admin preview capability, and a redesigned storefront (product detail pages, search, SEO, dark-brand theme).

**Current state**: the operational core (invoicing, fulfillment, tracking) is Production Validated. Real payment processing (Stripe test-mode, ACH, saved cards) and real bulk customer communications (Twilio SMS) are Engineering Complete but their production activation switches are deliberately OFF (`docs/PaymentReadiness.md`). Storefront checkout is gated behind `STOREFRONT_CHECKOUT_ENABLED`, unset in production as of the most recent readiness report (2026-08-08).

---

## 2. My Role

All engineering on this project was AI-assisted/orchestrated via Claude Code, directed session-by-session by the owner. This case study describes that division of labor honestly rather than presenting it as traditional solo manual coding:

- **Product strategy and vision**: defined what Pepscore Lab is becoming and in what order (`docs/ProductRoadmap.md`'s phase structure), including the decision to keep the marketing site (`pepscore-landing`) and the operational app (`pepscore`) as two permanent, separate Vercel projects rather than a single cutover domain (`ProductRoadmap.md`, "Landing Page & Domain Migration Strategy," updated 2026-07-22).
- **Requirements authorship**: wrote the original master development prompt that founded the invoice system (`docs/InvoiceSystemSpec.md`, Parts 1 & 2 — system role, design language, data model requirements, sample invoice data), and issued the specs behind later phases (Customer Identity Platform, Portal Adoption Automation, the payment-provider migration, Phase 3).
- **Architecture direction**: made or approved the load-bearing calls — e.g., directing that the acquisition-offer system reuse Pepscore's existing promotion architecture pattern rather than a disconnected coupon system, while explicitly leaving the exact model shape ("create a separate campaign abstraction if that remains the cleanest architecture after implementation-level review") to be resolved once an audit determined whether the existing invoice-discount `Promotion` model was structurally the same concept — it wasn't, so a new `PromotionCampaign`/`PromotionCode` pair was built instead, per that same standing direction (`docs/Decisions.md` #41); and that self-checkout should keep the existing `Order`+`Invoice` pairing rather than introduce a third sale-record shape (`ProductRoadmap.md`, "Two open architectural decisions").
- **Business-rule and pricing decisions**: e.g., the payment-arrangement availability rule (Decision #16), the portal-eligibility "needs a real invoice" rule and its explicit override via `leadStatus: CONVERTED` (Decisions #35, #36) — both are documented as directly challenged and corrected by the owner mid-session.
- **UX direction**: flagged the invoice module's initial branding mismatch against the real landing site and directed the dark-theme rebuild (Decision #10); set the "less is more," printer-friendly PDF branding standard (Decision #12).
- **QA and production-validation authority**: defined the "Code Complete vs. Production Validated vs. Complete" distinction now used across the roadmap (`ProductRoadmap.md`, "How completion is defined"; also `git log` #39, "Define Code Complete / Production Validated / Complete for the roadmap").
- **Security/compliance boundaries**: set and enforced the standing rule that real money movement, real bulk customer communication, and real payment/postage activation require explicit owner approval — reiterated verbatim in the Phase 3 sequencing constraints (`ProductRoadmap.md`, "Sequencing and safety constraints") and in every kill-switch decision in this log.
- **Implementation**: performed by Claude Code under this direction — writing code, running tests, fixing bugs found during that work, and proposing (not unilaterally deciding) architectural tradeoffs that the owner then approved, challenged, or overrode.

---

## 3. Problem / Opportunity

Before this build-out, Pepscore had a working Stripe storefront and an admin dashboard for order management, Shippo labels, and expense tracking (`docs/ProjectOverview.md`) — but no system for the manual/off-platform sales that were, in practice, a real share of the business. Every such sale required ad hoc tracking outside the app. There was no invoicing tool, no unified payment-status view spanning both sale types, and no CRM memory of a customer across their DM/cash history versus their Stripe account. The opportunity was to build one operational core that both sale paths feed into, so the owner gets one accurate picture of revenue, balances, and fulfillment regardless of how a sale happened — and to build it so each subsequent module (CRM, portal, inventory, storefront redesign) extends that core instead of duplicating it.

---

## 4. Product Requirements — How They Evolved

The requirements did not stay static; several were directly challenged and corrected mid-build, on the record:

- **Invoice data model** (frozen spec, `docs/InvoiceSystemSpec.md`) called for "saved locally for now" with future DB flexibility — the owner's engineering judgment (Decision #2) skipped the localStorage detour entirely since Postgres/Prisma was already provisioned, on the reasoning that building a throwaway layer to replace later would itself be the "quick hack" the spec explicitly forbade.
- **Invoice branding** initially inherited the pre-existing `/admin` dashboard's light theme by default. The owner flagged that this didn't match the real, live `pepscore-landing.vercel.app` site (all-black, not just the hero) — the invoice UI was rebuilt end-to-end to the measured, real brand tokens (Decision #10).
- **Payment arrangements** were originally spec'd to appear only on invoices already at `Partial` status (a literal reading of "should not appear for Pending or Paid invoices"). Live use surfaced a real gap — the owner wanted to set up a plan proactively on a brand-new Draft invoice "just in case it needs to be utilized" — and the rule was generalized to any invoice with a balance due (Decision #16).
- **Portal-invite eligibility**: the first rule excluded any customer with zero invoices as a "bare lead" (Decision #35, based on an actual production audit finding 6 of 17 real Customer rows were lead-stage). The owner directly challenged this — an admin-arranged sale in progress with no invoice yet is still a real customer — leading to the `leadStatus: CONVERTED` override (Decision #36), described in the log as "exactly the call the owner made."
- **Portal rollout automation** moved from a one-time frozen eligibility snapshot to live, ongoing eligibility on explicit owner instruction that "activated" should mean an ongoing state, not a single approved batch (Decision #37).
- **Phase 3 scope** (Promotion Campaigns, pricing intelligence, reorder) was pinned mid-session specifically because it "supersedes a hardcoded FIRST10 assumption" the owner wanted generalized before it shipped further (`ProductRoadmap.md`, Phase 3A).

---

## 5. System Architecture

**Stack**: Next.js 16 (App Router), TypeScript, Prisma ORM over Neon Postgres, Clerk (auth), Stripe (payments), Shippo (shipping/tracking), Resend (transactional email), Twilio (SMS), Tailwind CSS, `@react-pdf/renderer` (PDF generation), Zustand (storefront cart state only), Zod (validation), Vitest (testing).

**Frontend**: Next.js App Router with server components fetching data and client components (`InvoiceBuilder`, section components) owning interactive form state. No global state library for the invoice module — deliberately, since the whole editing session lives on one page (`docs/Architecture.md`).

**Backend / layering discipline**: a strict three-layer rule enforced throughout — UI components never call Prisma directly, API routes contain no business logic (auth check → parse/validate → call a `lib/` function → return), and each domain has exactly one data-access module (`lib/invoices.ts` is "the ONLY module that queries Prisma for invoice data," per `docs/Architecture.md`).

**Database**: Neon Postgres via Prisma, 51 models spanning storefront (`Product`, `Order`, `OrderReservation`), invoicing (`Invoice`, `InvoiceItem`, `InvoiceDiscount`, `InvoicePayment`, `PaymentArrangement`), CRM (`Customer`, `CustomerActivityLog`, `IntakeLink`, `LeadCapture`), payments (`Payment`, `AchAuthorization`, `SavedPaymentMethod`, `PaymentSettings`), fulfillment/tracking (`Shipment`, `TrackingEvent`, `ShipmentNotification`, `InvoiceActivityLog`), and portal identity (`CustomerPortalInvite`, `CustomerIdentityReviewCase`, `PortalRolloutSettings`). Schema changes are pushed via `prisma db push` — the log itself flags that a real migration history hasn't been adopted yet (Decision #40 section, `ProductRoadmap.md` Phase 2G).

**Auth**: Clerk. Admin auth is a single hardcoded `ADMIN_CLERK_USER_ID` check (documented as fine for one operator, explicitly flagged as needing real roles once there's more than one — `ProductRoadmap.md` Phase 2G). Customer Portal auth is a separate, newer authorization boundary (Customer Identity Platform, PRs #70–#89) scoping every query to the session's linked `Customer.id`.

**Payments**: Stripe behind a `PaymentProviderAdapter` interface (Decision #28) mirroring the existing `ShippingProvider` pattern — one interface, one real adapter today, additional providers (ACH via another processor, PayPal) are new files, not new branches through checkout code. Card, ACH (Decision #30), Cash App Pay, and PayPal are wired; Apple/Google Pay ride on Card automatically; Venmo has no Stripe Checkout path and is a permanently readiness-only flag (Decision #32).

**Inventory & pricing**: case/SPA/bulk/individual-vial sell-unit pricing (the "Pricing MVP," PR #91), a physical stock ledger with reservations (`InventoryReservation` for invoices, a separate `OrderReservation` for storefront Orders — Decision #29), and catalog-level backorder support with admin-discretionary accommodation (PR #107–#109).

**Customer identity**: two still-separate identities — `User` (Clerk-linked storefront account) and `Customer` (invoice/CRM identity, no login) — flagged as an open architectural decision Phase 2B must resolve (`ProductRoadmap.md`).

**Customer Portal**: shell, navigation, dashboard, invoice/payment/fulfillment/correspondence views, profile, support, saved payment methods, and real storefront Order history (PRs #70–#125). Built on the same `lib/` service layer the admin app uses — "no parallel customer version of `lib/invoices.ts` or `lib/customers.ts` should ever be written" (`ProductRoadmap.md`, Customer Portal Readiness Assessment).

**Admin**: dashboard, invoice management, CRM/leads interface (PR #110), fulfillment/package-preset settings, payment settings, portal rollout controls, notification-recipient configuration.

**CRM / leads**: `Customer`, `CustomerActivityLog`, `CustomerStatus` (computed), lead capture (`LeadCapture`, `FirstOrderOfferConfig`/`Claim`), duplicate-customer detection (`findPossibleDuplicateCustomers()`).

**Messaging**: Resend for transactional email (invoice-issued, payment-received, shipment notifications, correspondence log), Twilio for SMS (opt-in/opt-out webhook, transactional sends) — no bulk-send path exists in the codebase at all (Decision #27, explicitly noted as a safety property, not a gap).

**Fulfillment / shipping**: `Shipment` as a true one-to-many under `Invoice` (Decision #24), a centralized Fulfillment Gate (`lib/fulfillment/gate.ts`), real Shippo rate-shopping and label purchase (`lib/fulfillment/labels.ts`) gated behind a separate kill-switch from the API key itself (Decision #26).

**External integrations**: Clerk, Stripe, Shippo, Resend, Twilio, Zippopotam.us (free/keyless ZIP lookup, Decision #19).

**Deployment**: Vercel, two separate projects — `pepscore-landing` (marketing, owns `pepscorelab.com`) and `pepscore` (the operational app, no attached domain yet, reachable only at its Vercel-assigned URL) — deliberately kept as two permanent interfaces to one shared backend, not a temporary state pending cutover (`ProductRoadmap.md`).

---

## 6. Key Technical Decisions

Pulled from `docs/Decisions.md`'s 47 logged entries — the subset with the most architectural weight.

### Extend `Invoice` rather than build a parallel `ManualInvoice` model (Decision #1)
**Problem**: the existing `Invoice` model was hard-wired 1:1 to a Stripe `Order`, but manual/off-platform sales needed invoicing too.
**Options**: extend `Invoice` (optional `orderId`) vs. a fully separate `ManualInvoice` model.
**Decision**: extend `Invoice`.
**Why**: one invoice concept, one dashboard, one PDF pipeline regardless of sale origin — avoids "which one do I query" ambiguity at every future call site.
**Tradeoff**: `Invoice` now carries fields irrelevant to either path individually; judged acceptable since both share far more lifecycle than they differ.
**Result**: this single decision is the foundation every later invoice/payment/fulfillment feature built on without a rewrite.

### Payment arrangements as real relational models, not JSON (Decision #15)
**Problem**: the feature needed to support future overdue detection, reminders, a customer portal, and finance reporting — all requiring per-installment queries.
**Options**: `Json` column on `Invoice`, vs. real `PaymentArrangement`/`PaymentArrangementInstallment` tables.
**Decision**: real relational tables.
**Why**: `WHERE dueDate < now() AND status = 'PENDING'` already answers "what's overdue" with no future migration needed.
**Result**: this seam is exactly what later features (the Customer Portal, fulfillment eligibility) built on without touching the schema again.

### Automated Paid → Archived transition via daily Vercel Cron, `paidAt` as a live countdown anchor (Decision #21)
**Problem**: needed QuickBooks/Stripe-style automatic archival with every listed reset trigger (edit, reversal, reopen, additional payment) restarting the countdown correctly.
**Decision**: recompute `paidAt` on every relevant write rather than store a separate mutable "archive date."
**Why**: exactly one source of truth for when the countdown started; can't drift from the invoice's actual payment history.
**Tradeoff**: "Overdue" has no true due-date field on `Invoice` — implemented as a documented pragmatic stand-in.
**Result**: later regression-tested as its own permanent suite (PR #124) after PRs #120–123 extended the same filter/archive system.

### Carrier-agnostic tracking on a `ShippingProvider` abstraction over Shippo (Decision #22)
**Problem**: spec required not hard-coding carrier logic into invoice components, and preferred a multi-carrier API.
**Options**: 4 separate direct-carrier APIs (USPS/UPS/FedEx/DHL) vs. one abstraction over the already-integrated multi-carrier aggregator (Shippo).
**Decision**: `ShippingProvider` interface, Shippo as the only implementation today.
**Why**: zero new vendor relationships, adding a carrier or swapping providers later touches only a registry file and one adapter — never the UI, webhook, or cron routes.
**Result**: directly reused as the template for the payment-provider abstraction two months later (Decision #28).

### `Shipment` becomes a true one-to-many, primary shipment always derived, never stored (Decision #24)
**Problem**: real physical packages for one invoice aren't always exactly one shipment (mistyped tracking, split packages, replaced labels); shipping labels spend real money, so "can this ship" needed exactly one enforced check.
**Options**: keep 1:1 plus a separate history log table, vs. store a `primaryShipmentId` pointer, vs. drop the unique constraint and derive "current" on read.
**Decision**: one-to-many, `getPrimaryShipment()` derives current state (most recent non-voided) rather than storing a pointer.
**Why**: a stored pointer can silently drift (e.g., a voided shipment without reassignment); a pure derivation function is always correct by construction and unit-testable with no database.
**Result**: the centralized Fulfillment Gate (`checkFulfillmentEligibility()`) built directly on top, enforced identically in the UI and inside the label-purchase service itself.

### Live-eligibility rollout cron, replacing a frozen activation snapshot (Decision #37)
**Problem**: the original portal-rollout automation intersected eligible customers against a snapshot list captured once at admin activation — correct for a one-time bulk-invite decision, wrong for "the owner should not have to remember to re-approve as new customers qualify."
**Decision**: compute eligibility fresh on every cron run; the old snapshot is now historical-record-only.
**Why**: direct owner instruction that "activated" should represent an ongoing approved state, not a one-time batch.
**Tradeoff**: an admin who wants a genuine one-time-only batch send no longer has that as a built-in mode; Pause is the closest equivalent.
**Result**: every other safety gate (kill switch, dry-run default, allowlist, per-run cap) stayed independently sufficient — this change only touched which eligible customers a live run considers, not whether or how many it's allowed to contact.

### Portal eligibility excludes bare leads, then is overridden by an explicit `leadStatus: CONVERTED` (Decisions #35, #36)
**Problem**: should a customer with zero invoices ever receive portal credentials automatically?
**Decision**: exclude zero-invoice customers by default (#35) — audit found 6 of 17 real production Customer rows were lead-stage, 4 sitting inside the "14 eligible" bulk-invite audience — then add an explicit admin-vouched override via the pre-existing `leadStatus: CONVERTED` field rather than inventing a new column (#36).
**Why**: reused an existing, already-documented "never auto-computed, admin-only" signal instead of adding a second field that could silently disagree with it.
**Result**: verified to change zero real outcomes at the time (eligible count stayed 10) while correcting the rule for the case the original version could get wrong.

### Promotion Campaign system built as a new model pair, not an extension of the existing invoice-discount `Promotion` (Decisions #41, #42)
**Problem**: the owner's original instruction was to "reuse the existing promotion architecture" for a generalized, admin-configurable acquisition-offer system (so the hardcoded FIRST10 10% could become an editable, replaceable campaign) — but an audit found the existing `Promotion` model was a structurally different concept: a flat, admin-picked discount applied ad hoc to one invoice line, with no dates, eligibility rules, scheduling, or per-customer code issuance.
**Decision**: a new `PromotionCampaign`/`PromotionCode` model pair, explicitly authorized by the owner once the audit distinction was clear ("create a separate campaign abstraction if that remains the cleanest architecture after implementation-level review"). `FirstOrderOfferClaim` gained a nullable `campaignId` and `promotionCodeId` rather than the claim flow being rewritten from scratch. A second slice then migrated the actual claim logic onto the new system: live campaign resolution, real unique code issuance inside the same transaction as the claim, and a real branded discount email.
**A real production bug caught mid-build**: adding an hourly cron for scheduled campaign activation failed the deployment outright — this Vercel project is on the Hobby plan, which rejects any cron scheduled more frequently than daily (the same class of bug this project had already fixed once before for a different cron). Caught by a failed preview deployment, not assumed; fixed by changing to a daily schedule.
**A second real bug caught mid-build**: once the claim flow's rewrite added a notification-sending import, `next build` failed — the Twilio SDK (Node-only, needs `net`/`tls`) ended up in a client-side bundle, because a pre-existing client component statically imported the server-only storefront footer. Fixed by splitting the module so the read-only "is the offer live" surface stays free of any notification dependency.
**Result**: verified against real Postgres (a disposable rehearsal test exercising the full activate/demote/expire-codes transaction and the claim/code-issuance transaction) and a real Resend send that had to be debugged once — a Gmail test address was rejected because the Resend account is still sandbox-restricted to its own verified address, a pre-existing infrastructure fact surfaced by this work, not a defect in it. Live-verified end-to-end in production afterward: a real campaign created, activated, its live storefront banner confirmed rendering the campaign's own copy (not a hardcoded percentage), then fully retired/archived/deleted and the master switch restored off.

### Payment-provider abstraction, Phase 1 (Decision #28)
**Problem**: the existing Stripe integration was real and working but entirely Stripe-shaped — a genuine bug was found in the audit: the webhook silently dropped `charge.refunded`, `charge.dispute.created`, and `payment_intent.canceled`, leaving `Payment.status` stuck at `SUCCEEDED` forever after a real refund.
**Decision**: `PaymentProviderAdapter` interface mirroring the proven `ShippingProvider` pattern; existing Stripe columns untouched, new fields purely additive.
**Result**: verified against a genuinely computed Stripe webhook test signature — not a mock — confirming a full refund correctly moves `Order.status` to `REFUNDED` while a partial refund/dispute deliberately does not (a real business decision left unmade on purpose, not defaulted silently).

### ACH / Pay by Bank — genuinely async lifecycle, one shared "mark paid" tail (Decision #30)
**Problem**: ACH doesn't settle synchronously like a card; treating `checkout.session.completed` as sufficient signal (as cards do) would mark an order paid, ship-eligible, and email a confirmation for a debit that hasn't cleared and might still bounce.
**Decision**: branch on `session.payment_status`; only `async_payment_succeeded`/`async_payment_failed` (which can fire days later) settle the order for real. `markOrderPaid()` extracted as the single function both the card and ACH-success paths call.
**Security boundary honored**: this database never receives, transmits, or stores a raw bank account/routing number — only Stripe's own safe references.
**Result**: verified with genuinely-signed Stripe webhook events covering the full lifecycle, including idempotency on redelivery.

### Shared email shell replaces 18 independently-styled templates, and admin-facing alerts join the same visual system rather than getting a second shell (Decisions #43–#47)
**Problem**: the storefront/admin/Customer Portal had already moved to a dark-gold brand system (Decision #10), but every customer-facing and admin-facing email template still rendered on the old cream/serif shell — a genuine brand-cohesion gap flagged directly, not assumed. An audit before building (per this project's established discipline) found 18 templates on the old system: 15 customer-facing, 3 admin-only.
**Decision**: one new module, `emails/shared/shell.ts`, owns the branded header (wordmark + gold divider), CTA buttons, elevated panel, and footer for every email; each template file keeps its own content/business logic and only calls into the shared presentation layer. Every color token was measured directly from the live production storefront via `getComputedStyle()` JS inspection, not invented (`#D4AF37`→`#E8C84A` gradient, `#000000`/`#0D0D0D` dark surfaces) — the same "measure the real site" discipline as Decision #10. Migrated across 5 reviewable slices (PRs #140–#144) rather than one large PR. Admin-only alerts, which previously used a visually distinct "PEPSCORE ADMIN" shell, were folded into the same shared shell (distinguished only by an "Admin Alert" eyebrow and footer note) rather than maintained as a second shell variant.
**Why**: a shared shell is what prevents the *next* new template from drifting again, which restyling each file independently wouldn't; one shared admin/customer shell is easier to keep in sync than two.
**Result**: all 18 templates (32 distinct rendering functions across them) now draw their header/CTA/panel/footer treatment from one source of truth. Every migrated rendering function was exercised for real via Resend (not mocked) to a sandbox-verified address for direct visual verification before merge. Full test suite stayed green throughout (626/626 by the close of the migration), and `next build` stayed clean at every slice, confirming the shared-shell import graph never leaked a server-only dependency into a client bundle (the exact failure class already fixed once in Decision #42).

### Storefront `Order` inventory reservation as a separate model from the invoice-scoped one (Decision #29)
**Problem**: audit for the ACH work found storefront checkout never reserved or deducted stock at all — two concurrent checkouts for the last unit could both succeed.
**Decision**: new `OrderReservation` model mirroring `InventoryReservation`'s proven discipline, rather than widening the mature, production-active invoice-scoped model to also serve a then-inactive storefront feature.
**Result**: verified against a real Stripe test-mode session reserving/rejecting/fulfilling/releasing stock correctly, confirmed via actual `physicalStockOnHand` changes, not inferred.

### PDF layout fix — `wrap={false}` moved from the whole legal footer to each clause (Decision #18)
**Problem**: a live invoice with a payment history and a 4-installment arrangement was overflowing to a nearly-empty second page.
**Root cause found**: the *entire* legal footer had `wrap={false}`, so react-pdf pushed the whole footer to page 2 the moment it was even a few points short of fitting on page 1 — a strictly worse failure mode than genuine content overflow.
**Decision**: move `wrap={false}` to each individual clause instead of the container.
**Result**: verified empirically by rendering real invoices and counting actual PDF page objects — every typical scenario now renders on exactly one page; only genuinely content-heavy edge cases use two.

### Manual tracking made Shippo-independent; backorder gating extended everywhere shipping status can move (Decision #25)
**Problem**: a hard `throw` inside what was meant to be an optional provider adapter broke the one fulfillment path (Pirate Ship + manual entry) actually used day-to-day in production — a real production bug.
**Decision**: manual tracking registration becomes best-effort; backorder gating extended from just Shippo label purchase to every path that can move an invoice's shipping status (manual tracking creation, webhook/poll cascade, admin overrides).
**Result**: manual tracking became "genuinely first-class rather than works-if-Shippo-happens-to-also-be-configured."

---

## 7. Major Features

| Feature | Description | Status |
|---|---|---|
| Invoice system core | Unlimited line items, stacking discounts/promotions, dual PDF output (Master/Client), sequential numbering, live preview | **Production Validated** |
| Payment arrangements | Weekly/biweekly installment schedules, available on any invoice with a balance, computed payment status | Production Validated |
| Invoice archiving | Auto-archive Paid invoices after a configurable delay via daily cron; regression-tested suite (PR #124) | Production Validated |
| Invoice trash | Soft-delete with recoverable two-step permanent delete | Production Validated |
| Manual invoicing | Customer/shipping capture, ZIP autofill, "same as billing" sync | Production Validated |
| Product catalog & pricing engine | Case/SPA/bulk/individual-vial sell units, manual-override-beats-formula precedence | Production Validated |
| Inventory & backorders | Physical stock ledger, invoice-scoped reservations, catalog-level backorder + admin accommodation | Production Validated |
| Sell units / SPA eligibility | Explicit wholesale/SPA pricing eligibility on the storefront | Production Validated |
| Customer identity (CRM) | `Customer`, `CustomerActivityLog`, computed `CustomerStatus`, duplicate detection | Production Validated |
| Customer intake workflow | Secure expiring links, public intake form, duplicate-detection/upsert pipeline | **Complete** (Phase 2A — Code Complete + Production Validated, incl. one continuous real-customer acceptance run per `ProductRoadmap.md`) |
| Customer Portal | Dashboard, invoices, payments, fulfillment tracking, correspondence, profile, storefront order history | Production Validated |
| Portal invitation & adoption automation | Secure claim flow, eligibility rules, live-eligibility rollout cron, delivery-failure surfacing | Production Validated |
| Carrier-agnostic shipment tracking | `ShippingProvider` abstraction, webhook + polling fallback, 16-value normalized status | Production Validated (registration path verified via sandbox + direct service calls, per Decision #22's documented limitation) |
| Fulfillment Gate | Centralized "can this invoice ship" check (paid, arrangement, or attributed override) | Production Validated |
| Shippo label purchase | Real rate-shopping and label purchase, printerless USPS QR codes | **Engineering Complete — Activation Pending** (`SHIPPO_PURCHASING_ENABLED` unset; account awaits Shippo Trust & Safety business-registration review, per Decision #26) |
| Search | Invoice search by customer/number/tracking/phone/SKU/amount; storefront product search | Production Validated |
| Promotion Campaign system | Admin-configurable acquisition-offer campaigns (percentage/fixed, draft→scheduled→active→retired→archived lifecycle, one default first-order campaign at a time, a stacking-compatibility engine with a Family & Friends privileged class) — generalizes what was originally a hardcoded FIRST10 offer | **Engineering Complete — Activation Pending** (master switch off in production; live-verified end-to-end via a real test campaign, then cleaned up) |
| FIRST10 lead capture | Full claim journey on the campaign system: lead form → dedup → unique single-redemption code issuance → branded discount email → correspondence logging | **Engineering Complete — Activation Pending** (`FirstOrderOfferConfig.enabled` off by default; real Resend delivery and code issuance verified via a disposable rehearsal test, not simulated) |
| Promotions (legacy invoice-discount catalog) | Reusable, stackable discount picker for manual invoice lines — a structurally separate concept from the Promotion Campaign system above, kept intentionally distinct rather than merged (`docs/Decisions.md` #41) | Production Validated |
| Payments — card | Embedded Stripe Checkout | **Engineering Complete — Activation Pending** (test-mode keys only; `STOREFRONT_CHECKOUT_ENABLED` off in production) |
| Payments — ACH / Pay by Bank | Async lifecycle, mandate evidence, never stores raw bank data | **Engineering Complete — Activation Pending** |
| Saved payment methods | Card + bank via Stripe Customer/SetupIntent, Customer Portal UI | **Engineering Complete — Activation Pending** |
| Bulk/transactional SMS (Twilio) | Sending real, STOP/START opt-out real and DB-backed; A2P 10DLC registration is the owner's remaining checklist | **Engineering Complete — Activation Pending** (`TWILIO_*` unset in production) |
| Admin controls | Payment settings, fulfillment settings, package presets, notification recipients, portal rollout controls | Production Validated |
| Analytics | Invoice dashboard KPIs, payment processing-cost analytics (`getPaymentCostAnalytics()`) | Production Validated (data-layer real; processing-cost analytics correctly shows empty state since checkout has been off) |
| Shared email design system | Centralized branded email shell (`emails/shared/shell.ts`) — header, CTA, panel, footer tokens measured from the live storefront; all 19 customer- and admin-facing email templates migrated onto it, plus a new admin preview page (32 sample-data-only renderings, no real send) | **Production Validated** (presentation-only migration; content/business logic of every template unchanged, verified via real Resend sends to a sandbox-verified address before each merge) |
| Price-change audit trail + preview | Per-tier price-change history (`ProductPriceChange`), a pre-commit diff/preview before an admin's catalog-wide price update saves, and a "Use Once vs Update Product Price" prompt on manual-invoice price edits | Production Validated |
| Admin individual-vial access | Admin invoice composition can select Individual Vial pricing independent of storefront `individualSalesEnabled` visibility, flagged "not on storefront" in the picker | Production Validated |
| Sell-unit-aware cart & reorder | Cart/checkout key on (product, sell unit) together, not product alone; Customer Portal Buy Again/Reorder All re-resolves every historical line against current price/availability, never a stale snapshot; admin-assisted reorder surfaces a customer's real purchase history on their profile with one-click "Add to New Invoice" | **Engineering Complete — Activation Pending** (checkout-facing half gated behind `STOREFRONT_CHECKOUT_ENABLED`; admin-assisted reorder is fully live and verified in production) |

---

## 8. UX Evolution

Sourced from `docs/Decisions.md` and `docs/UIUXGuidelines.md` — no design history is claimed beyond what these documents actually record.

- **Initial invoice UI**: launched on the pre-existing `/admin` dashboard's light `cream`/`g100` convention, inherited by default rather than checked against the real brand (Decision #10).
- **Dark rebrand**: the owner flagged the mismatch against the live `pepscore-landing.vercel.app` site, which live JS inspection confirmed was pure black end-to-end (`rgb(0,0,0)`, not just the hero) with flat-depth, hairline-bordered glass cards (`bg-white/[0.03]`, `border-gold/10`, `rounded-[18px]`, no shadow) — not the filled-gray-box treatment the invoice UI had inherited. The invoice dashboard and builder were rebuilt to these measured tokens, centralized in one file (`components/invoices/theme.ts`) so a future token change is a one-file edit. The rest of `/admin` (non-invoice pages) was initially left on the old light theme, a known gap called out directly in the decision log — since closed by a full admin dark-theme migration (2026-08-07) with its own automated regression guard (`scripts/check-admin-theme.ts`), confirmed clean during the Phase 3D final brand-cohesion audit (Decision #60).
- **Global notification design system**: a centralized branded email shell (`emails/shared/shell.ts`) replaced 19 templates' worth of ad hoc inline styling with one shared header/CTA/panel/footer token set, measured from the live storefront the same way the original dark rebrand was (Decisions #43–47, #49, #59). Brand-name consistency was then audited and corrected across every surface that renders it — the in-app Customer Portal, admin notification cards, and real SMS bodies — settling on one consistent spelling (`Pepscore Lab`) everywhere it's actually rendered (Decisions #57, #58, #59, #60), plus a new admin-only preview capability for every template using synthetic sample data, no real send required (Decision #59).
- **PDF branding**: restrained, deliberate use of the gold accent (a small header bar, the status badge, section-label underlines) rather than color-coding every status, on the explicit standard that PDFs "reinforce the brand but remain professional and printer-friendly" and must never look like marketing material (Decision #12).
- **PDF layout iteration**: went from an initially generous, branding-focused layout to a tightened one after real invoices were found overflowing to a nearly-empty second page — root-caused and fixed (Decision #18), verified by rendering real invoices and counting actual PDF page objects rather than guessing.
- **Interaction conventions**: no native browser `confirm()` dialogs anywhere in the dashboard — destructive actions (Delete, Delete Forever) use an in-page two-click arm/confirm pattern consistent with the app's toast-driven interaction language (Decision #20).
- **Accessibility**: at least one direct fix is on record — screen-reader labels corrected in the "Correct Sell Unit" admin dialog (`git log`, PR #95, "Scenario 18 follow-up: fix screen-reader labels").
- **Mobile responsiveness**: at least one direct fix is on record — "Fix mobile-responsiveness gaps in admin page headers and two grids" (`git log`, PR #55). Beyond these specific commits, this document does not have evidence of a systematic, page-by-page mobile/accessibility audit — that would need to be confirmed separately before being claimed at that scope.
- **Storefront redesign**: dedicated product detail pages, category pages, search, SEO architecture (sitemap/robots/structured data), and a "dark Pepscore Lab theme migration" for the storefront itself (`git log`, PRs #99, #100, #101, #103, #104) — bringing the storefront in line with the same brand tokens the invoice module adopted earlier.

---

## 9. Significant Bugs / Challenges

Pulled from `docs/Decisions.md`'s and `docs/ChangeLog.md`'s documented self-caught issues — real, not illustrative.

**Stripe webhook silently dropping refund/dispute/cancellation events.** *What happened*: an audit ahead of the ACH work found `charge.refunded`, `charge.dispute.created`, and `payment_intent.canceled` were never handled — a real refund from the Stripe Dashboard left `Payment.status` permanently stuck at `SUCCEEDED`. *Why it mattered*: financial state could silently disagree with reality indefinitely. *Root cause*: the webhook handler was built only for the original `checkout.session.completed` happy path. *Fix*: the new `PaymentProviderAdapter`/reconciler now handles all three. *Verification*: a genuinely computed Stripe webhook signature (not a mock) drove a seeded payment through a full refund (confirmed `Order.status` → `REFUNDED`), a partial refund, and a dispute (Decision #28). *Lesson*: an integration that only handles the success path is a latent data-integrity bug, not a finished feature.

**Storefront checkout never reserved inventory, for any payment method.** *What happened*: discovered during the ACH prerequisite audit — two concurrent checkouts for the last unit of a product could both succeed. *Why it mattered*: a real overselling risk the moment checkout goes live. *Fix*: a new `OrderReservation` model, transactionally reserved at Order creation, released on Stripe failure (Decision #29). *Verification*: a real Stripe test-mode session confirmed rejection of a checkout that would exceed remaining stock, and confirmed actual `physicalStockOnHand` deduction on fulfillment.

**Product picker silently resolved to the wrong strength.** *What happened*: the invoice line-item picker matched on bare product name (`product.name`), but the catalog reuses names across strengths (Tesamorelin 5mg/10mg). Selection could silently resolve to whichever row happened to come first, and even a correct selection dropped the mg strength from the saved line item. *Why it mattered*: real invoices could show the wrong price/strength with no visible indication. *Fix*: a composed `Name — Size — 1 Box` label (`formatProductLabel()`), verified to have zero collisions across all 119 live catalog products (Decision #13).

**Legal footer's `wrap={false}` pushed the entire footer to a near-empty second page.** *What happened*: found while verifying that a real invoice (payment history + 4-installment arrangement) fit on one page — it didn't. *Root cause*: `wrap={false}` on the whole footer container, not per-clause, so react-pdf moved the entire block rather than filling remaining space on page 1. *Fix*: moved `wrap={false}` to each individual legal clause (Decision #18). *Verification*: rendered real and representative invoices, counted actual PDF page objects.

**Manual tracking (Pirate Ship) broke in production because of an unrelated dependency.** *What happened*: a hard `throw` inside what was meant to be an optional Shippo adapter call broke the one shipping workflow actually used day-to-day, whenever Shippo couldn't register a tracking number. *Why it mattered*: this wasn't a theoretical edge case — it was the live production path. *Fix*: made provider registration best-effort with a non-fatal warning surfaced in the response (Decision #25).

**Vercel Hobby-tier cron rejection silently blocked every production deploy.** *What happened*: `poll-tracking`'s cron was scheduled every 4 hours; Vercel's Hobby tier rejects the *entire deployment* if any cron runs faster than daily — this had been silently blocking every production deploy since the carrier-tracking PR merged. *Fix*: changed to once-daily, matching the archive sweep (`docs/ChangeLog.md`, "Sanitized Shippo API errors + Vercel Hobby cron fix"). *Lesson*: a platform-tier constraint that silently blocks deploys is a production incident waiting to be found, not a cosmetic config detail.

**PDF header logo failed to embed / dates rendered a day early.** *What happened*, found during initial invoice-system smoke testing: the logo needed a base64 data URI, not a bare filesystem path, to embed in `@react-pdf/renderer`; and payment/delivery dates rendered a day early due to local-timezone formatting of UTC-midnight dates. *Fix*: base64 embedding; UTC forced in `lib/invoice/format.ts`, shared by both PDFs and the live preview (`docs/ChangeLog.md`, "Initial build").

**Zero-invoice eligibility rule was directly challenged and found to be too strict.** *What happened*: Decision #35's "needs at least one invoice" portal-eligibility rule was correct as a safe default but would have wrongly excluded a real customer an admin was actively arranging a sale for. *Fix*: `leadStatus: CONVERTED` override (Decision #36), reusing an existing admin-only CRM field rather than inventing a new one. *Verification*: all 4 real customers the original rule excluded were individually audited by name and confirmed to genuinely be unconverted leads before the fix shipped — the fix changed zero real outcomes at the time while correcting the rule.

**An ordering bug in failure-summarization logic, caught during its own code review.** *What happened*: `summarizeRepeatedFailures()`'s naive last-write-wins logic over an array of failures silently picked the *oldest* error as "most recent" when the caller's query returned newest-first — because the oldest run sits last in that array. *Fix*: compare `occurredAt` explicitly instead of relying on iteration order (Decision #40). *Lesson*: array-position assumptions about "most recent" are a recurring class of bug worth a specific regression test, which this fix added.

**A new cron silently would have blocked every future production deploy.** *What happened*: a new cron for scheduled Promotion Campaign activation was initially scheduled hourly — the PR's own preview deployment failed outright rather than degrading silently. *Root cause*: this Vercel project is on the Hobby plan, which rejects the entire deployment if any cron runs more frequently than daily — the exact same class of platform-tier constraint that had already caused a real production incident earlier in the project (a 4-hour tracking-poll cron silently blocking every deploy until caught). *Fix*: changed to a daily schedule, matching every other cron in the project. *Lesson*: a lesson from earlier in the project (state the platform constraint explicitly rather than rediscovering it) wasn't fully internalized as a checklist item — worth naming as a gap in process, not just a one-off fix.

**A server-only dependency leaked into a client bundle and broke the production build.** *What happened*: rewiring the FIRST10 claim flow to send a real discount email added an import chain that transitively pulled in the Twilio SDK (Node-only, requires `net`/`tls`). `next build` failed. *Root cause*: a pre-existing client component (`CheckoutForm.tsx`) directly imports the storefront `Footer` — a Server Component — so Next.js's client bundler follows that import regardless of `Footer`'s own server-only body, and any heavy server-only dependency anywhere in `Footer`'s import graph breaks the client build. This was a latent, pre-existing risk that had simply never been triggered before, since the original `Footer`/FIRST10 code had no notification-sending import at all. *Fix*: split the module so the read-only "is the offer live" surface stays free of any notification dependency, isolating the email-sending code (and therefore Twilio) to a separate file the client bundle never touches. *Lesson*: a passing build today doesn't prove an import boundary is actually enforced — it can mean the risky path just hasn't been exercised yet.

---

## 10. Security / Reliability

- **Kill switches, layered and independent of key presence**: `STOREFRONT_CHECKOUT_ENABLED` (checkout unreachable client- and server-side while unset), `SHIPPO_PURCHASING_ENABLED` (blocks label purchase independent of whether an API key is configured — deliberately, to avoid a stale test key silently "working" and being mistaken for live, Decision #26), Twilio configuration checks (every SMS resolves to `SKIPPED_NOT_CONFIGURED` while `TWILIO_*` is unset, and the inbound webhook itself 404s with no token to verify against, Decision #27).
- **Dry-run defaults**: the portal-invite rollout cron defaults to dry-run, with an allowlist and per-run cap as additional independent gates (Decision #37's "Benefits" section).
- **Idempotency**: Stripe webhook handlers guard against redelivery via `stripePaymentIntentId` existence checks, extended to every new handler added this session and verified via rehearsal tests that explicitly redelivered events and confirmed no duplicate rows (Decision #28, #30). Tracking events are deduplicated at the database layer via a `(shipmentId, eventHash)` unique constraint (Decision #22).
- **Provider abstraction as a reliability pattern**: both `ShippingProvider` (Decision #22) and `PaymentProviderAdapter` (Decision #28) exist specifically so a provider swap or addition never touches UI/webhook/cron code — reducing the blast radius of any single integration's failure or migration.
- **Payment tokenization**: no raw card or bank account/routing number, online-banking credential, or Financial Connections token is ever received, transmitted, or stored by this database — Stripe's own hosted/embedded UI collects it, and only safe references (PaymentMethod id, last4, brand) come back (Decision #30, #33).
- **ACH mandate evidence**: a dedicated `AchAuthorization` model records mandate id, authorization version, timestamp, IP/user agent, and revocation state — never overwritten (Decision #30).
- **Access controls**: admin routes gated by a single `isAdmin` check (`userId === ADMIN_CLERK_USER_ID`); a regression guard exists specifically to catch any admin API route missing that check (`git log`, PR #68, "Add regression test guarding every admin API route's isAdmin() check"). Customer Portal routes use a separate, purpose-built authorization boundary scoping every query to the session's linked `Customer.id`, with an explicit rule that admin-only fields (`internalNotes`, discount presets, balance-transfer execution) must never round-trip through a customer-facing response (`ProductRoadmap.md`, Customer Portal Readiness Assessment).
- **Audit logging**: `AdminAuditLog` for admin actions, a dedicated `InvoiceActivityLog` for shipment/tracking events (deliberately separate since its shape needs an optional `userId` to cover webhook/system-sourced entries, Decision #22), `CustomerActivityLog` for CRM events, and a cron-run audit trail (`ROLLOUT_CRON_RUN`, `REMINDER_CRON_RUN`) that surfaces repeated portal-invite delivery failures to the admin UI (Decision #40).
- **Timing-safe comparisons**: the Shippo webhook shared-secret comparison was hardened to timing-safe (`git log`, PR #29) after being flagged in the Phase 2 completion audit.
- **Environment/secrets handling**: `/admin/settings/payments` shows only booleans ("Stripe Configured," "Test Mode," "Checkout Enabled") derived server-side — never a secret value in the browser (Decision #31). `CRON_SECRET`-gated cron endpoints. Preview and Production share one Neon database by deliberate choice (`docs/ChangeLog.md`, "PR #1 preview troubleshooting").
- **Real-money/communication activation gates**: reiterated as a standing rule throughout the entire payment-provider arc — "no real money moved, no real postage was purchased, and no real bulk customer message was sent at any point this session. Every live-activation switch remains in its safe, fail-closed default state" (`docs/PaymentReadiness.md`, verified as of 2026-08-08, PRs #107–#119).

---

## 11. Testing / QA

- **Unit tests**: **1,401 tests passing across 128 test files**, run via Vitest (`npx vitest run`, executed directly against this repository at the time this revision was written — up from 626 tests / 59 files at the previous case-study revision). Vitest itself was added specifically for the carrier-tracking work — "no test framework existed in this project before" (`docs/ChangeLog.md`) — starting from 47 tests and growing with each subsequent feature (pure business logic: schedule generation, eligibility computation, status normalization, dedup, format validation). See "Phase 9," "Testing architecture and regression growth" for how the most recent doubling happened: a dedicated launch-readiness coverage audit (~147 new tests across 14 previously-untested pure-logic modules), the Pepscore Intelligence AI epic's own safety-matrix and per-capability suites (150+ tests), and the Finance Center's 32 new pure-function tests, all following the same established discipline of testing pure business logic directly rather than mocking Prisma/DB orchestration.
- **Type checking**: `npx tsc --noEmit` — clean, verified directly at the time this case study was written.
- **Linting**: `npx eslint .` — reported as clean at every PR per `docs/ChangeLog.md`'s verification notes; a real flat-config compatibility bug (Decision #9) was found and fixed early in the project specifically because Next.js 16 removed the `next lint` subcommand, meaning `eslint .` had never actually been exercised as a path until then — fixing it surfaced 4 genuine findings in code the same PR touched.
- **Build**: `npm run build` (`prisma generate && next build`) — reported clean at every PR per the ChangeLog's verification notes.
- **Rehearsal / disposable scripts against real provider test APIs**: the dominant verification pattern for payment/shipping/SMS work — genuinely-signed Stripe webhook events, real Shippo test-mode purchases, a genuinely computed Twilio request signature — rather than mocks, with explicit call-outs whenever something *couldn't* be verified end-to-end in this environment (e.g., no real browser can complete an embedded Stripe checkout or a Financial Connections bank-linking flow from a script — acknowledged directly in Decisions #30, #32, #33 rather than glossed over).
- **Live/production verification**: multiple documented end-to-end passes on the actual deployed app — the invoice system's full workflow tested on the live Preview deployment before merge (`docs/ChangeLog.md`, "PR #1 preview troubleshooting"); Shippo tracking taken live end-to-end in production for the first time with a real USPS tracking number resolving 16 real historical events (`docs/ChangeLog.md`, "Sanitized Shippo API errors + Vercel Hobby cron fix"); the single continuous real-customer acceptance run closing out Phase 2A (`ProductRoadmap.md`).
- **Permanent regression suites**: added after specific incidents, not just ad hoc scripts — e.g., a permanent regression suite for invoice archiving after PRs #120–123 (`git log`, PR #124), and a regression guard confirming every admin API route enforces `isAdmin()` (PR #68).

---

## 12. Outcomes / Metrics

Only what is actually measurable from this repository's own evidence — no business metrics (revenue, customer counts, conversion rates) are claimed, because they are not yet available:

- **303 commits on `master`** (`git log --oneline | wc -l`, run directly against this repository at the time this revision was written). Through PR #195 this project tracked work as numbered, merged pull requests (143 through the previous case-study revision, 195 by the end of Phase 4); the AI epic and later sprints (Phases 7–9, 55 commits) shipped as direct, individually-authored commits to `master` rather than numbered PRs — a real workflow shift worth naming plainly rather than inventing PR numbers that don't exist for that work.
- **74 logged architecture decisions** in `docs/Decisions.md`, each with Decision/Reason/Alternatives/Benefits/Drawbacks — current through Decision #74 (PR #193); the 55-commit gap this backfill covers predates a Decisions.md entry, since that log was not updated in step with that work (see "Continuous Update Rule").
- **1,401 passing unit tests** across 128 test files (Vitest), grown from 47 tests at the introduction of the test framework and 626 tests at the previous case-study revision.
- **67 Prisma models** in the current schema (`grep -c "^model " prisma/schema.prisma`), spanning storefront, invoicing, CRM, payments, fulfillment, portal identity, first-party AI compliance/usage/retrieval, and financial reporting/tax.
- **112 API routes** (`app/api/**/route.ts`), of which **76** live under `app/api/admin/**` and are covered by an automated regression guard (`adminAuthCoverage.test.ts`) confirming every one enforces `requireAdmin()`; a companion `adminPageAuthCoverage.test.ts` covers the **29** admin pages the same way.
- **6 registered Vercel Cron jobs** (`vercel.json`) plus one built, safety-gated, and deliberately not yet registered (`first-order-offer-reminders`).
- **7 external integrations**: Clerk (auth), Stripe (payments), Shippo (shipping/tracking), Resend (transactional email), Twilio (SMS), Zippopotam.us (ZIP lookup), and Vercel AI Gateway (Anthropic + Google model routes, ZDR-verified, not yet publicly activated).
- **`npx tsc --noEmit` clean** at the time this document was written.
- Phase 2A (Customer Intake Workflow) is the one phase explicitly marked **Complete** in `docs/ProductRoadmap.md` — both Code Complete and Production Validated, including a single continuous real-customer acceptance run.
- **Adoption and revenue metrics**: real storefront checkout, real bulk SMS, and real ACH/saved-payment-method activation remain switched off in production. The business's actual current sales channel — admin-created invoices — is live and operating: **$3,903.00 in recognized revenue across 18 genuine invoices** as of the Finance Center sprint (2026-08-18), corrected from a previously-reported $4,978.00 that was found to include $1,075.00 from 11 leftover test/rehearsal invoices (see Phase 9's Financial Reporting Data-Integrity mini case study). This is the first real, quantified revenue figure this document has been able to report — every prior revision stated plainly that no live transaction volume existed yet.

---

## 13. Lessons Learned

- **Audit before building, every time.** Several of the highest-value fixes in this project's history were found by auditing existing behavior before writing new code, not by executing a feature request literally — the Stripe webhook refund/dispute gap (Decision #28), the missing storefront inventory reservation (Decision #29), the zero-invoice portal-eligibility gap (Decision #35), and the "no event-driven trigger needed" conclusion (Decision #38, which explicitly chose *not* to build something because the audit showed the existing daily cron plus manual button already covered the real need).
- **A working provider abstraction pattern is worth reusing verbatim.** `ShippingProvider` (Decision #22) was built first; `PaymentProviderAdapter` (Decision #28) is explicitly described as mirroring it. Investing in the pattern once paid off a second time.
- **Derive, don't store, whenever a value could drift.** This shows up repeatedly and deliberately: computed `PaymentStatus` (Decision #17), the derived primary `Shipment` (Decision #24), live-recomputed `CustomerStatus`-based portal eligibility (Decision #35/#37) — each explicitly rejected a stored flag/pointer in favor of a pure function precisely because a stored value can silently disagree with reality.
- **A failure mode that fails *silently* is worse than one that fails loudly.** The Vercel Hobby-tier cron rejection blocking every deploy, the Stripe webhook silently dropping refund events, and the manual-tracking `throw` breaking the actual production shipping workflow are all versions of the same lesson: an integration point that degrades silently is far more dangerous than one that visibly errors.
- **Real verification means hitting the real (test-mode) API, not a mock.** The recurring pattern across every payment/shipping/SMS decision — genuinely-signed webhook events, real Stripe test-mode sessions retrieved back from Stripe's own API, a genuinely computed Twilio signature — reflects a deliberate standard that a mocked auth check or a hand-typed expected response isn't real verification.
- **State the limitation instead of glossing over it.** Every "couldn't be verified end-to-end in this environment" case (embedded checkout completion, Financial Connections bank linking, live-carrier Shippo registration under a sandbox key) is documented explicitly rather than silently assumed to work — this discipline is itself a project artifact worth naming.
- **AI-assisted engineering requires an owner who reviews and challenges, not just approves.** The two clearest examples — the payment-arrangement availability rule (Decision #16) and the portal-eligibility override (Decision #35/#36) — exist because the owner caught a real gap between what the spec said and what the actual workflow needed, and corrected it. The audit trail this produced (a full Decisions.md log) is itself a byproduct of directing AI-assisted development deliberately rather than treating it as autonomous.

---

## 14. Future Roadmap

Clearly separated from completed work — pulled from `docs/ProductRoadmap.md`, which pins sequencing rationale ahead of scope.

- **Phase 2D — Inventory & Purchasing** (Planned): real stock counts decremented on order/invoice creation, low-stock alerts (unblocks Phase 2B's self-checkout from overselling); full scope includes supplier tracking, purchase orders, batch/lot tracking for research compounds, warehouse locations.
- **Phase 2E — Automation** (Planned): abandoned-cart emails, shipment follow-ups, review requests, reorder reminders, return-customer discounts — riding entirely on the existing notification dispatch system, no new infrastructure needed.
- **Phase 2F — Analytics Dashboard** (Planned): extends the existing `getInvoiceDashboardStats()` seam — revenue, best-sellers, repeat-customer rate, AOV, gross margin, payment-method mix.
- **Phase 2G — Compliance & Security, later items** (Planned): two-factor authentication, session management, real admin roles (today a single hardcoded admin id), formal audit-log expansion, automated backups/secrets rotation, a real Prisma migration history to replace `db push`. (Note: several *earlier* Phase 2G items — Stripe webhook idempotency, the `SHIP_FROM_STREET` mismatch, basic rate limiting, timing-safe Shippo webhook comparison — are already done, per `ProductRoadmap.md`.)
- **Phase 3A — Configurable Promotion Campaign system + FIRST10 completion** (Engineering Complete — Activation Pending): the `PromotionCampaign`/`PromotionCode` schema, lifecycle service, stacking-compatibility engine, and admin management UI shipped (`docs/Decisions.md` #41), then the FIRST10 claim flow was migrated onto it end to end — live campaign resolution, unique code issuance, and a real branded discount email (`docs/Decisions.md` #42) — verified live in production via a real test campaign (created, activated, storefront banner confirmed, then fully cleaned up) and a disposable rehearsal test hitting real Postgres and real Resend delivery. `FirstOrderOfferConfig.enabled` (the master switch) stays off in production, so the entire flow remains structurally inert pending an explicit owner go-ahead. Twilio double opt-in remains **not started**, deliberately deferred to its own slice pending an audit of the exact production Twilio/A2P-registered consent wording.
- **Phase 3B — Admin pricing intelligence + individual-vial admin access** (**Complete**): a full per-tier price-change audit trail (`ProductPriceChange`, Decision #48), admin individual-vial access independent of storefront visibility (Decision #50), a "Use Once vs Update Product Price" prompt on manual-invoice price corrections (Decision #51), a pre-commit diff preview on the admin pricing panel (Decision #52), and a direct audit confirming pricing and physical-inventory writes never cross into each other's fields (Decision #53). PRs #146, #148–#150.
- **Phase 3C — Customer Portal reorder ("Buy Again") + admin-assisted reorder** (**Complete**): the storefront cart became sell-unit-aware (Decision #54, and found/fixed a real inventory-reservation bug along the way — a 2-case order was only ever reserving 2 vials instead of 20), Customer Portal Buy Again/Reorder All re-resolve every historical line against the *current* product state and only ever add to the cart, never touch checkout directly (Decision #55), and admin-assisted reorder surfaces a customer's real purchase history on their profile with one-click "Add to New Invoice," live-verified in production (Decision #56). PRs #151, #152, #154.
- **Phase 3D — Global PepScore Lab notification/communication design system** (**Complete**): all 19 customer- and admin-facing email templates migrated onto the shared shell (Decisions #43–#47, #59); brand-name consistency audited and corrected across the in-app Customer Portal, admin notification cards, and real SMS bodies, settling on one consistent spelling everywhere it renders (Decisions #57, #58, #59, #60); a new admin-only email-template preview capability using synthetic sample data (Decision #59); and a final audit confirming the broader dark-frontend visual hierarchy was already sound, closing the one remaining casing gap in the shell itself (Decision #60). PRs #156, #158, #160, #161, #163.
- **Open architectural decision, now resolved**: `User` and `Customer` were originally two separate, unconnected identities (see "5. System Architecture" above) — `Customer.userId` and a self-service claim/resolve flow (`lib/portal/selfServiceResolve.ts`) now link them, with admin-invite, self-service-claim, and self-registration all converging on the same linking transaction.
- **Standing safety constraint for all future phases**: real bulk customer communication and real payment/checkout activation stay off throughout continued engineering work; only a defined short list of triggers (real money movement, security issues, blocking factual business questions) should stop autonomous execution.

---

## 15. Evidence Index

Every claim below traces to a real PR number or commit found in `git log`, cross-referenced against `docs/Decisions.md`/`docs/ChangeLog.md`. No PR number in this table is invented.

| Area | PR / Commit | Evidence | Result |
|---|---|---|---|
| Invoice system foundation | #1 (`8f9b97e`) | Decision #1–#14, `ChangeLog.md` "Initial build" | `Invoice` extended with items/discounts/payments; dual PDF generation; dashboard + builder shipped |
| Invoice branding / dark theme | #2 (`28ca541`) | Decision #10, #11 | Dashboard/builder moved to measured landing-page dark tokens |
| Dropdown contrast fix | #3 (`c2900c1`) | — | Fixed invisible white-on-white text in dark-theme selects |
| Payment arrangements | #4, #5 (`15ee3a4`, `68980f3`) | Decision #15, #16 | Real relational installment model; generalized beyond Partial-only invoices |
| PDF one-page fit | #6 (`8f59aff`) | Decision #18 | Root-caused footer `wrap` bug; verified via real PDF page counts |
| ZIP autofill / address sync | #7 (`937b88d`) | Decision #19 | Zippopotam.us proxy; ephemeral same-as-billing state |
| Invoice trash | #8 (`610aa42`) | Decision #20 | Soft-delete + two-step permanent delete, DB-enforced ordering |
| Automated archival | #9 (`09426d8`) | Decision #21 | Daily Vercel Cron sweep, `paidAt` as live countdown anchor |
| PDF logo enlargement | #10 (`e1cf353`) | `ChangeLog.md` | Verified against real invoices, still 1 page |
| Carrier-agnostic tracking | #11 (`342ae71`) | Decision #22 | `ShippingProvider` abstraction over Shippo; 47 unit tests added (first test framework) |
| Hobby-tier cron fix | #12 (`67fd4e8`) | `ChangeLog.md` | Fixed a bug silently blocking every production deploy |
| Sanitized Shippo errors | #13 (`9f95ad2`) | `ChangeLog.md` | Live Shippo production go-live: real USPS tracking, 16 real events |
| Automatic invoice-issued email | #14 (`f0e47e1`) | Decision #23 | Gated by activity-log presence, not a boolean flag |
| CRM foundation | #15 (`95ccbd3`) | — | `Customer`, intake links, activity log, notifications schema |
| Customer activity wiring | #16 (`0f97bb9`) | — | Wired into existing invoice touchpoints |
| Notifications subsystem | #17 (`35bad10`) | — | Dispatch, dashboard bell, email channel, SMS stub |
| Shipment one-to-many refactor | #18 (`9c4bdb3`) | Decision #24 | Dropped unique constraint; derived primary shipment |
| Fulfillment Gate | #19 (`d3a29ae`) | Decision #24 | Centralized "can this ship" check |
| Real Shippo label purchase | #21 (`fb0595b`) | Decision #24, #26 | Rate-shop + buy postage, DB write only after Shippo confirms |
| Payment Received email | #23 (`4951d5a`) | — | No dedup guard by design (each payment is a distinct event) |
| Fulfillment Workflow v1 doc | #24 (`704f79e`) | `ChangeLog.md` | Multi-shipment, gate, label purchase documented |
| Product Roadmap adopted | #25 (`65a1a1f`) | `ProductRoadmap.md` | Live Phase 2 planning document established |
| Stripe webhook idempotency fix | #26 (`09ea1c2`) | `ProductRoadmap.md` Phase 2G | Pulled forward from audit findings |
| Basic rate limiting | #28 (`0646358`) | `ProductRoadmap.md` Phase 2G | Checkout, both webhooks, zip-lookup |
| Timing-safe Shippo comparison | #29 (`aff0eab`) | `ProductRoadmap.md` Phase 2G | Hardened webhook auth |
| Customer Intake Workflow (Phase 2A) | #30–#36 | Decision set, `ProductRoadmap.md` | Marked **Complete** — real acceptance run |
| Customer Identity Platform | #70, #81–#89 | — | Portal identity linking, claim flow, security tests |
| Customer Portal shell → full views | #71–#73, #125 | — | Dashboard, invoices/payments/fulfillment, storefront Order history |
| Portal rollout kill-switch | #74 (`cd6fe6c`) | — | `PORTAL_ENABLED` |
| Auth Sprint (P1–P10) | #75–#80 | — | Clerk webhook, audit trail, authorization regression guard |
| Pricing / Inventory MVP | #91 (`964584d`) | — | Case/SPA/individual pricing, physical stock ledger, reservations |
| Backorder model | #93, #107–#109 | — | Reservation lifecycle, admin correction UI, catalog-level backorder |
| Payment-provider abstraction | #113 (`0ec9f52`) | Decision #28 | `PaymentProviderAdapter`, refund/dispute reconciliation fixed |
| Storefront inventory reservation | #114 (`ffa7cda`) | Decision #29 | `OrderReservation`, verified against real Stripe test session |
| ACH / Pay by Bank | #115 (`3b99da9`) | Decision #30 | Async lifecycle, `AchAuthorization`, no raw bank data stored |
| Admin Payment Settings + analytics | #116 (`3127b1b`) | Decision #31 | Real gates vs. readiness-only flags, live `groupBy()` analytics |
| Embedded Checkout + PayPal | #117 (`04e0133`) | Decision #32 | Venmo confirmed to have no Stripe path |
| Saved payment methods | #118 (`694f354`) | Decision #33 | Stripe Customer/SetupIntent reuse of embedded-Checkout component |
| Payment-change safety | #119 (`d413c43`) | Decision #34 | Stale Checkout Session invalidation on backorder-driven balance change |
| Payment readiness report | (`9bd5eab`) | `PaymentReadiness.md` | All live switches confirmed off, dated 2026-08-08 |
| Twilio A2P 10DLC readiness | #112 (`ef014d2`) | Decision #27 | Real STOP/START webhook, signature-verified |
| Portal eligibility fixes | #126–#134 | Decision #35–#40 | Duplicate-phone gap, lead-stage exclusion + override, live rollout cron, delivery-failure surfacing |
| Phase 3 roadmap pinned | #135 (`63af5c4`) | `ProductRoadmap.md` | Promotion Campaigns, pricing intelligence, reorder — queued, not yet built |
| Promotion Campaign architecture | #136 (`03ff2a9`) | Decision #41 | `PromotionCampaign`/`PromotionCode` schema, lifecycle service, stacking engine, admin UI — additive, zero behavior change to existing FIRST10 flow |
| Case study backfill | #137 (`8184bc8`) | This document's own history | Initial `docs/CaseStudy.md`, spot-checked against real `git log` before shipping |
| FIRST10 migrated onto Promotion Campaign system | #138 (`95b052e`) | Decision #42 | Live code issuance, real discount email, module split to fix a real client-bundle build failure; live-verified in production then cleaned up |
| Shared email shell + slice 1 (InvoiceIssued, PortalInvite) | #140 (`4c85ace`) | Decision #43 | `emails/shared/shell.ts` built from live-measured tokens; first 2 templates migrated |
| Email shell slice 2 (PaymentReceived, OrderConfirmation, RefundNotice, FirstOrderOfferCode refactor) | #141 (`b3dabec`) | Decision #44 | 5 rendering functions migrated |
| Email shell slice 3 (BackorderNotice, BalanceTransferNotice, TrackingUpdate, InvoiceShipmentUpdate) | #142 (`219487e`) | Decision #45 | 7 rendering functions migrated |
| Email shell slice 4 (AchPaymentProcessing, ClientArrangementDecision, ClientSubmissionConfirmation, ContactInquiry) | #143 (`d76b04d`) | Decision #46 | 7 rendering functions migrated |
| Email shell slice 5 — closing (IntakeLinkRequest, PortalSupport, all 3 admin-only templates) | #144 (`d0d0330`) | Decision #47 | 9 rendering functions migrated; completes the 18-template Phase 3D email migration |
| P logo mark added to email shell | #147 (`93617e0`) | Decision #49 | Email-safe derivative of the invoice P monogram, real alpha transparency |
| Price-change audit history | #146 (`c6237fd`) | Decision #48 | `ProductPriceChange` per-tier history, wired into the sole write path |
| Admin individual-vial bypass | #148 (`cfa32d9`) | Decision #50 | `adminContext` opt-in on `getAvailableSellUnits()`, zero change to existing callers |
| Smart invoice price correction | #149 (`8c8b77f`) | Decision #51 | "Use Once vs Update Product Price" prompt; live-verified in production, price reverted after |
| Global pricing preview + Pending Owner Actions doc | #150 (`2105465`) | Decision #52 | Pre-commit diff modal; `docs/PendingOwnerActions.md` created as the one consolidated blocker index |
| Pricing/inventory separation audit | (docs-only) | Decision #53 | Audited every write path in both domains — confirmed already clean, closes Phase 3B |
| Sell-unit-aware cart architecture + reservation bug fix | #151 (`314b16f`) | Decision #54 | `resolveCheckoutLine()`, cart keys on (product, sellUnit); found and fixed a real 10x-under-reservation bug, verified via a real-Postgres rehearsal script |
| Customer Portal Buy Again / Reorder | #152 | Decision #55 | `resolveReorderLine()` always re-resolves current price/availability; Buy Again/Reorder All only ever touch the cart, never checkout |
| Admin-assisted reorder | #154 | Decision #56 | "Previously Purchased" section on the admin customer profile, one-click "Add to New Invoice"; live-verified in production against a real customer's purchase history — closes Phase 3C |
| In-app Customer Portal + admin notification-card audit | #156 | Decision #57 | Brand-name consistency fixed across 6 portal surfaces; gold-accented toast styling; admin notification bell confirmed already clean |
| SMS brand-name consistency audit | #158 | Decision #58 | 5 real SMS send sites corrected; required opt-out language left byte-for-byte untouched |
| Casing correction + LeadCaptured shell migration | #160 | Decision #59 | Corrected this session's own `PepScore Lab` → `Pepscore Lab` regression to match the dominant live convention; last un-migrated template moved onto the shared shell |
| Admin email template preview capability | #161 | Decision #59 | 32 template renderings, synthetic sample data only, sandboxed iframe (`sandbox=""`), zero real sends |
| Final brand-cohesion audit | #163 | Decision #60 | Resolved the shell's own `<title>`/`alt` casing gap; confirmed the broader dark-frontend hierarchy already sound via the existing admin-theme regression guard + live screenshots — closes Phase 3D |
| Regression test coverage | #124, #68 | — | Invoice-archiving suite; every admin route's `isAdmin()` check |
| Inventory-reservation race + negative-stock fix (Phase 4A Critical) | #168 | Decision #61 | `withOptimisticProductLock()` applied to all 9 inventory write sites; verified against real Postgres |
| Admin Online Storefront Order management + safe cancellation (Phase 4A Critical) | #169 | Decision #61 | Cancellation respects paid/fulfilled state; verified against real Postgres |
| Abandoned-checkout reservation leak fix (Phase 4A Critical) | #170 | Decision #61 | Stripe `checkout.session.expired` webhook + 25-hour reconciliation cron |
| Server-side promotion redemption at checkout (Phase 4A Critical) | #171 | Decision #62 | Two-phase soft-hold; verified against real Postgres, full lifecycle including idempotent finalize |
| AOAI flagship-alignment audit + web/funnel analytics foundation | #172 | Decision #63 | `@vercel/analytics` + first-party event catalog; verdict FLAGSHIP ALIGNED WITH MINOR GAPS, zero P0 |
| Footer dead-link cleanup | #172 | Decision #64 | Replaced dead `#`/`/terms`/`/privacy` links with non-interactive labels; no fabricated legal copy |
| Security/concurrency triage: timing-safe cron auth, portal-invite race, stale comment | #173 | Decision #65 | Closes Phase 4A exit criteria — every Medium/Low finding dispositioned |
| Phase 4B journey tracing + checkout-Invoice linkage/item fix | #175 | Decision #66 | `Invoice.customerId`/`InvoiceItem` gap fixed; two further findings routed to Phase 4N/4D; resolved a stale `PendingOwnerActions.md` #8 |
| Live production incident: unconfigured image host crashed the homepage | #178 | Decision #67 | Found and fixed during Phase 4B prep — real homepage 500 in production, root cause + systemic fix + 8 regression tests |
| Phase 4C/4D/4F first passes + Mark as Delivered | #182 | Decision #68 | Portal isolation verified, admin friction gap fixed, security spot-check clean; 3 findings routed to 4E/4M/4O |
| Phase 4G: cap concurrent unpaid checkouts per email | #184 | Decision #69 | Reservation-hoarding guard, verified via real Postgres + running dev server |
| Phase 4H performance fixes + Phase 4I live accessibility verification | #186 | Decision #70 | aggregate()/groupBy() replacing unbounded reduce; 2 missing indexes; live keyboard-nav/focus verification via browser automation |
| Phase 4J/4K status + brand-casing fix | #188 | — | Real regression found and fixed (Shop PepScore Lab → Pepscore Lab) |
| Phase 4M/4N-4T re-audit (docs-only) | — | Decision #71 | PaymentReadiness.md addendum; evidence-based status for 6 sub-phases; new PendingOwnerActions #10 |
| Phase 4U launch checklist + 4V reconciliation + 4W real UI rehearsal | — | Decision #72 | New docs/LaunchReadiness.md; PendingOwnerActions re-verified clean; real checkout UI click-through, one finding flagged for clean re-verification |
| Phase 4N genuine-gap fix: storefront checkout wired into existing backorder architecture | #192 | Decision #73 | `applyBackorder()` now called on any nonzero shortfall against a backorder-enabled product; verified against real Postgres via synthetic fixtures (safe-merge and normal-checkout regression both PASS) |
| Phase 4E genuine-gap fix: customer-merge dead end replaced with safe-merge-or-explicitly-blocked | #193 | Decision #74 | `lib/customers/merge.ts`; 3 real identity-conflict conditions detected and refused rather than guessed through; verified against real Postgres for both safe-merge and blocked-merge outcomes; closes the last genuine engineering gap named in the Phase 4X report |
| Docs reconciliation for both resolved pre-pilot gaps | #194 | Decisions #73/#74 | ProductRoadmap/LaunchReadiness/CaseStudy updated; recommendation intentionally held at READY FOR CONTROLLED PILOT |
| Final admin operational UX audit: customer search normalized-phone matching + Storefront Orders visibility | #195 | — | Reused existing `phoneNumbersMatch` convention rather than inventing a new one; added the one missing profile section; verified via real-Postgres rehearsal, then live against a real production customer record post-deploy |
| Item-level invoice refunds | `668f2be` | This document, "Phase 7" | Proportional discount allocation; closed two pre-existing uncapped-refund gaps found mid-audit |
| RBAC migration | `7e03940` | Mini Case Study: RBAC Migration | Centralized `lib/auth/rbac.ts`; replaced 67+9+18 duplicated inline admin checks; regression guard rewritten to match |
| Admin predictive customer search | `0cbd866` | This document, "Phase 7" | New `adminPageAuthCoverage.test.ts` closes a page-level auth-coverage gap the route-level guard didn't cover |
| Catalog taxonomy / FDA-FTC risk-reduction cleanup | `fc1a182`, `811632d` | This document, "Phase 7" | 17 product descriptions + 6 category labels + 1 raw-category fix corrected to neutral RUO-compatible language |
| Family-image resolver + approved asset import | `2d9bd66` | Mini Case Study: Asset/Catalog Standardization | 64 owner-approved family images wired; real `next/image` space-plus-space bug found and fixed |
| Asset organization (labels/manifests/audits/archive) | `102d207` | Mini Case Study: Asset/Catalog Standardization | 117 print labels relocated out of `public/`; full audit trail preserved at `docs/assets/` |
| Mobile navigation event-handling fix | `0c63e7b` | Mini Case Study: Mobile Navigation Event-Race Defect | `mousedown`/`click` race root-caused and fixed; real `user-event` regression test added, confirmed to fail pre-fix |
| Pepscore Intelligence AI-0B foundation | `47321e6` | This document, "Phase 8" | Zero-dependency AI foundation; verified by grep to have zero non-`lib/ai` importers; 34-test safety matrix |
| AI-1.1–AI-1.9 (first-party analytics, structured intelligence, Admin AI Control Panel) | `1eee510`–`3d20afa` | This document, "Phase 8" | First-party search/engagement capture; compare/discover/explain capabilities; read-only-then-actionable observability dashboard |
| AI-1.10/1.11 (retrieval + provider-router wiring) | `8325558`, `3c719c0` | This document, "Phase 8" | Closed two "built but never connected" gaps found during roadmap reconciliation |
| AI-1.12–AI-1.17 (live-model integration, ZDR, kill-switch split) | `d0c53a1`–`260b679` | Mini Case Study: Vercel AI Infrastructure Migration | Hobby→AOAI Pro transfer; team-scoped credential mismatch found and resolved; `AI_LIVE_MODEL_ENABLED` split from `AI_FEATURE_ENABLED` |
| AI-1.18: Tier 1 catalog retrieval fix for natural-language questions | `2d5ccb4` | Mini Case Study: AI First-Party Grounding Defect | `rankSearch` mismatch found via retrieval trace; research-specific adapter added; true end-to-end pipeline regression test |
| Launch-readiness test-coverage sweep | `9108e05`–`d84bc8d` | This document, "Phase 9" | 14 modules, ~147 new tests; testing convention (pure logic only) explicitly confirmed rather than assumed |
| Launch-readiness safety/correctness + SEO-domain-normalization sprints | `85958ea`, `c5832a9`, `2d44e37` | This document, "Phase 9" | Missing Shippo kill-switch on Order-side label route closed; Stripe idempotency key added; sitemap/robots/canonical domain fallbacks normalized |
| Rehearsal-invoice archival | `c3c88d5` | Mini Case Study: Financial Reporting Data-Integrity Defect | 11 test invoices archived via existing reversible mechanism, not deleted |
| Launch checklist consolidation | `eefeb17` | This document, "Phase 9" | 5 new owner-decision docs; 2 earlier findings corrected on re-verification |
| Finance Center: ledger, tax reporting, accountant exports | `210e3bc` | Mini Case Study: Financial Reporting Data-Integrity Defect | `Invoice.isTestData`/`Order.isTestData` added; revenue corrected $4,978→$3,903; 11-sheet→13-sheet export |
| Overview vs. Finance Center revenue-figure clarification | `72a5baf` | This document, "Phase 9" | Documented two legitimately different revenue definitions rather than forcing false agreement |
| Case study backfill through `72a5baf` + portfolio layer + architecture diagrams | `2ad291f` | `docs/PortfolioCaseStudy.md`, `docs/ArchitectureDiagrams.md` | 55-commit documentation gap closed same day it was found |
| Finance/Tax P0 verification: test-data-leak/`deletedAt` fix, formula-injection guard, `ORDER_WITHOUT_PAYMENT` flag | `a68438f`, `598cc1c`, `6579ba3` | Mini Case Study: The Second Test-Data Leak | Live Gross Revenue corrected $1,432.00→$1,431.00; 6 additional untagged test invoices found and disclosed; 1,409/1,409 tests passing |
| Owner-approved 6-invoice `isTestData` backfill applied | `c31b370` | This document, "Phase 9" | 20 Discounts & Credits rows (down from 22), zero remaining test-customer matches |
| Finance P1: QuickBooks/Xero export + Estimated Tax Planning + P1 roadmap audit | `600af53`, `fde75da`, `a8dd5f1` | This document, "Phase 10" | Two capabilities shipped without paid/external dependencies; 6 blocked items documented with exactly what each needs |
| Admin cleanup: notification-bell/nav collision, 10 missing table scroll wrappers, 1 missing modal scroll cap | `3fe85b6`, `54fa1d4`, `d602c11` | Mini Case Study: The Notification Bell / Nav Collision | Live-verified via production screenshot; zero dead links/placeholder copy/orphaned routes found across the full admin audit |

---

## Phase 5 (in progress): Homepage/Catalog/Compliance Refresh Sprint (started 2026-08-12)

**Status: preview branch (`homepage-refresh-sprint`), not yet merged** — intentionally held for owner visual approval before merging to `master`, per this sprint's own explicit instruction. Not added to the Evidence Index table above, which is reserved for merged/deployed work; will be backfilled there once merged.

Scope built so far: consolidated homepage catalog directory (a real, accessible, keyboard-navigable clickable component replacing the old static-image catalog concept, built from `oldcatalog.png` as a structural reference only), homepage product-priority reordering (GLP-1 → NAD+ → CJC-1295/Ipamorelin → GLOW70 → Tesamorelin, derived from the live product catalog, never invented), a homepage-embedded search bar, an original rewrite of the hero paragraph the owner flagged as too close to another site's wording, a sitewide COA-claim correction (removed "accompanies every order" language not supported by actual business policy, replaced with "COA Certified Products" per owner direction), and a versioned RUO acceptance architecture (`lib/compliance/ruo.ts`, `ComplianceAcknowledgment.version`) so a signed-in customer accepts the current disclaimer once rather than being re-interrupted every order, while guests still accept per checkout. A mid-sprint owner addendum added authentication-security work: relabeled the header sign-in CTA to "Customer Sign In" with an explicit customer-intent redirect signal (reusing the existing `?portal=customer` pattern already built for the landing page, rather than duplicating it), and added a discreet "Admin Sign In" footer link — both routing through the app's single existing Clerk sign-in page, preserving the single-identity/server-side-authorization model rather than building a second auth system.

Genuine gaps found and routed to `docs/PendingOwnerActions.md` rather than guessed through: Clerk phone/SMS sign-in factor is not currently configured (confirmed by direct inspection of the rendered sign-in UI — Google OAuth + email/password only), so step-up/MFA policy is a Clerk Dashboard action, not an app-code gap; and the old catalog reference's "Botulinum Toxin" entry has no corresponding live product, so it was correctly omitted from the new directory rather than invented.

**Revision pass #2 (2026-08-12, same preview branch)**: a customer-facing merchandising taxonomy (`lib/storefront/merchandisingTaxonomy.ts`) now sits alongside the database's own `Product.category` field as a pure presentation layer — the eleven owner-directed research-domain categories (GLP-1/Weight Management, Growth Hormone/Performance, Recovery/Injury, Blends/Stacks, etc.), each mapped to real product names so a product can belong to more than one category without any duplication in the database. `/categories` and `/categories/[slug]` were rebuilt against this taxonomy (richer gold-accented panel treatment with per-category icons), `ProductCard`'s category link and `app/sitemap.ts` both moved off the old raw-category slug helper (deleted as dead code once nothing referenced it), and the catalog directory was redesigned into a genuine metallic "gold bar" tray holding individually engraved dark tiles, each still a real, crawlable `<Link>`. A direct database audit (not assumption) found the "CJC-Ipamorelin without DAC" and "Botulinum Toxin" products the owner was looking for already exist in the catalog but are marked inactive, and that KLOW is a real, distinct, currently-inactive blend — all three routed to `docs/PendingOwnerActions.md` as a reactivation decision rather than fabricated or silently reactivated. The six owner-approved individual-vial products (Semaglutide, Tirzepatide, Retatrutide, NAD+, Glutathione, PT-141) had their `individualSalesEnabled` flag turned on against their existing stored prices, matching GLOW70's prior configuration exactly — auditing further surfaced that the storefront has no actual "buy an Individual Vial instead of a case" purchase selector yet (Add to Cart only ever adds the Standard Case, even for GLOW70), a real, separate feature gap now logged rather than glossed over. Also removed the homepage's redundant full pricing-reference table, the site's remaining cold-chain shipping claims (products ship lyophilized, not cold-chain), deactivated Bacteriostatic Water from the sellable catalog per owner instruction (soft-deactivated, not deleted, preserving any historical order references), and published five real policy pages (Terms, Privacy, Returns & Refunds, Shipping, Lab Results/COA) as pre-launch drafts explicitly flagged for owner/legal review rather than the previous placeholder "Coming soon" footer labels.

**Revision pass #3 (2026-08-12, same preview branch)**: closed the Individual-Vial purchase-flow gap logged at the end of pass #2 -- `ProductCard.tsx` now renders a real Standard Case / Individual Vial toggle, and a live rehearsal script confirmed the selection correctly threads through the pre-existing sell-unit-aware cart/checkout/reservation architecture (`resolveCheckoutLine()`, `computeVialsToReserve()`, both already built and tested from Decision #54's earlier reorder work) for all 8 public-vial products, while correctly rejecting a public Individual Vial request on all 4 hidden-vial products (Botulinum Toxin, KLOW, Tesamorelin, AOD-9604). Reactivated the three real-but-inactive products identified in pass #2 (CJC-1295-without-DAC+Ipamorelin, Botulinum Toxin, KLOW) using formula pricing derived from a fresh owner-supplied wholesale source, with Botulinum Toxin's unit corrected to "100 units" (was mislabeled "100mg"). Applied explicit owner-directed manual price overrides to Glutathione (3 sizes), AOD-9604, TB-500 (2 sizes), and HCG (2 sizes), and fixed Cerebrolysin's `unitsPerCase` (10 -> 6, a real special case), which corrected its Individual Vial price from a wrong-divisor $52 to the correct $86. A full audit of the remaining 62 active, non-locked catalog products against the wholesale-sourced formula found **zero discrepancies** -- every stored price already exactly matches `supplier cost x 8.0521/5.6748/10.7362`, meaning the earlier pricing pass that built this catalog was already fully correct against this same wholesale source; if pricing still looks too high, that's the formula's own multipliers, not a data error, and was left for an explicit owner/commercial decision rather than unilaterally changed. Also rebuilt the catalog directory so the full outer container reads as worked gold metal (not just a gold border around a dark panel), corrected the site's bulk-order marketing copy from fabricated percentages to the owner's real 4-tier discount schedule, and fixed the About section's quality-claim wording per owner direction.

**Revision pass #4 (2026-08-12, same preview branch)**: found and fixed the real root cause behind the owner's "strengths are visibly missing" report -- `ProductCard.tsx` only ever showed a product's size via the variant-selector pills, which don't render at all for a single-strength product (the majority of the catalog), so MT-2, Dermorphin, HMG, and a dozen others silently showed no strength anywhere on the card despite the database being correct all along. Fixed once at the shared component (homepage, category pages, and search results all consume it), then verified against real server-rendered HTML for all 14 owner-named products rather than trusting the database state alone. Retired the old per-item pricing formula (supplier cost x8.0521/x5.6748/x10.7362) by explicit owner instruction and replaced Standard/SPA with a new model (supplier cost x4, commercially rounded to the nearest $10; SPA = Standard x a ~0.705 ratio derived directly from Retatrutide's own real approved per-strength pricing) -- applied across all 63 non-protected active products via a live recalculation script, with KLOW given its own explicit $960 override matching the owner's worked example. Fixed a real SPA-pricing invariant violation (SPA priced above Standard Case for AOD-9604/TB-500/HCG after their manual overrides) and added a hard `SpaPricingInvariantError` guard in the admin pricing-update service so the same contradiction can never be silently written again. Built a reusable `ScientificBackground` component wrapping an owner-provided DNA/molecular art asset and applied it at graduated intensities across the hero, catalog directory surround, About section, category pages, product pages, and portal header -- never full-opacity, always faded before reaching text. Upgraded the sitewide flat 2-stop gold gradient to a richer metallic 3-6 stop treatment (new `.bg-gold-luxury`/`.text-gold-luxury` tokens in `app/globals.css`) across all 16 files that used the old pattern. Corrected two further instances of a false "Pepscore Lab synthesizes/manufactures" claim (hero and About copy) alongside the flagged "Founded by scientists" line, replacing all three with owner-approved language distinguishing Pepscore Lab (supplier) from the certified pharmaceutical laboratory (producer). Generated `docs/MasterPricingList-2026-08-12.csv`/`.md` (101 active SKUs, public and hidden rates) and built a ready-to-call `sendMasterPricingReport()` function with a branded email template and CSV attachment -- prepared, never auto-sent, awaiting explicit owner pricing approval per `docs/PendingOwnerActions.md` #19.

**Revision pass #5 (2026-08-12, same preview branch)**: found and fixed the actual cause of a merchandising complaint ("CJC-Ipamorelin-without-DAC appears oddly next to KLOW") -- the blend product had never been added to any homepage priority tier or given an explicit category-page sort order, so it fell through to raw database-creation-date order on both surfaces, landing next to KLOW by coincidence rather than by design. Fixed at the root: `lib/storefront/homepagePriority.ts`'s CJC tier now explicitly sequences Ipamorelin -> the blend -> the other CJC-1295 variants, and `app/categories/[slug]/page.tsx` now sorts by the merchandising taxonomy's own product-name order instead of `createdAt` (a general improvement, not a one-off patch). Carried the "Sterile Manufacturing" and "Wholesale Partnerships" trust cards over from the landing page into the existing Features/Trust card grid (reused the component, no new one built), wiring Wholesale Partnerships to the same wholesale-inquiry lead-capture flow the Bulk section already uses. Extended the `ScientificBackground` system with `zoom`/`flip` props so the same single source asset reads as a varied, art-directed composition (DNA helix at the hero, molecular-bond texture flipped for the Products section, particle-field motif for Bulk, a mirrored corner treatment for Features, and a final subtle watermark in the Footer) rather than the same image tiled down the page. Finalized the RUO acknowledgment on owner-provided copy -- two distinct required checkboxes (21+ age confirmation, RUO/Terms/Privacy agreement with real links to the now-live `/terms`/`/privacy` pages) replacing the single combined statement, versioned `RUO-2026-01` (previous `v1` text preserved in a code comment for comparison rather than a separate document) -- which automatically requires every existing customer to re-accept once under the new version, by the versioning architecture's existing design, without any additional code change.

**Revision pass #6 (2026-08-12, same preview branch)**: fixed a real scroll glitch traced to a `LeadCaptureTrigger` nested inside the Features grid's hover-transform card -- removed that CTA (informational-only now, per owner direction, not replaced) rather than patch around it, since the Bulk section above already carries the homepage's one wholesale-inquiry CTA. Found the header's sticky "Pricing" nav link was a dead anchor -- pointed at `#pricing`, a section removed in an earlier pass -- and removed it from both desktop and mobile nav rather than leaving a broken link standing. Renamed the Bulk section's CTA from "Inquire About Bulk Orders" to "Inquire About Wholesale Orders" to consolidate on one term. Strengthened the `ScientificBackground` system globally: roughly doubled the `medium`/`subtle` opacity tiers (hero's `strong` tier left untouched, preserving the already-approved first section exactly), narrowed and lightened the edge-fade masks that were washing out too much artwork on scroll, promoted several sections (Products, Bulk, Catalog Directory, Footer) from subtle to medium, and gave the Features and About sections a bolder `strong` treatment each -- one full unzoomed DNA helix (About, mirrored to the page's left edge) and one zoomed molecular-bond moment (Features) as the two "sometimes stronger" beats beyond the hero the owner asked for. Added a new Contact-section watermark to close the one remaining gap in the background flow between the gold CTA banner and the Footer.

**Revision pass #7 (2026-08-12, same preview branch)**: swapped the hero and Mission-section product photography to two new owner-supplied compositions (`newPShero.png`/`newPShero2.png`, both 1672x941 -- a materially different aspect ratio than the previous square/near-square crops), correcting each `<Image>`'s width/height so next/image sizes them at their true ratio instead of distorting into the old box. The hero's `animate-float` treatment (Tailwind `float` keyframe, unchanged) was explicitly preserved -- an asset swap, not an animation change -- while the Mission-section image stayed stationary as it already was, keeping the two placements deliberately different. Also closed a real accessibility gap found while touching this: the float animation had no `prefers-reduced-motion` handling at all; added a `@media (prefers-reduced-motion: reduce)` rule in `app/globals.css` that disables `.animate-float` for anyone with that OS/browser preference, with zero effect on the animation for everyone else.

**Revision pass #8 -- AOAI Flagship SEO + Lead Capture Optimization Sprint (2026-08-12, same preview branch)**: audited the existing lead-capture/promotion/SEO infrastructure first, per explicit instruction not to duplicate systems -- the audit (an `Explore` subagent pass) found the `PromotionCampaign` dynamic-offer system, `FirstOrderOfferClaim`'s `@unique customerId` dedup, the reminder-cron safety-gate pattern (`lib/portal/reminderSafety.ts`), the analytics event catalog, and the structured-data/sitemap/robots builders were already correctly built, narrowing real remaining work to four genuine gaps. Fixed a real dedup bug: lead and first-order-offer email capture normalized whitespace/case inconsistently, so `Jane@Example.com` and `jane@example.com` could both claim the same offer -- both validation schemas now `.toLowerCase()` the email before dedup. Added a near-hero first-order-offer banner reading from the same live `PromotionCampaign` data the modal already used (no hardcoded "FIRST10"). Added `websiteSchema()` (WebSite+SearchAction) and `productGroupSchema()`/`isVariantOf` structured data so multi-strength products link as a real ProductGroup rather than N unrelated Products, IDs always derived from the real `Product.name`. Built the first-purchase reminder cron end-to-end (`lib/promotions/firstOrderReminderEligibility.ts`'s day-2/day-5 cadence, `app/api/cron/first-order-offer-reminders/route.ts`) by directly reusing `lib/portal/reminderSafety.ts`'s existing dry-run/kill-switch/allowlist/cap engine rather than building a second one -- deliberately **not** registered in `vercel.json`'s cron schedule, so merging it sends zero real reminders until explicitly authorized. A same-day addendum renamed "Customer Sign In" to "Client Sign In" and added a dismissible coach-mark tooltip pointing at it; the addendum's own follow-up correction identified that the actual mobile-header collision was the gold Cart button crowding the "Lab" wordmark (not the sign-in CTA as first reported) -- fixed by giving the header nav a larger enforced minimum gap below the `md` breakpoint only (where the desktop nav that normally provides that spacing is hidden), leaving desktop/tablet spacing untouched.

**Revision pass #9 -- Hero image refresh + public copy/Mission correction (2026-08-12, same preview branch)**: swapped the hero and Mission images again to owner-corrected versions of the same two source files -- since the filenames were unchanged from the prior pass, both were saved under new versioned filenames (`pepscore-hero-v2.png`/`pepscore-mission-v2.png`, confirmed by checksum to be genuinely different bytes from the previous deploy) specifically to defeat CDN/browser caching rather than trust a same-name overwrite to propagate. The corrected Mission image's real dimensions (1536x1024) turned out to differ from the previous file's 1672x941, so its `width`/`height` props were corrected too -- shipping the old dimensions would have silently stretched the new artwork. Removed the redundant "Pepscore Lab" text eyebrow above the hero heading (duplicated the header wordmark immediately above it). Removed all three public-facing "GMP-compliant" mentions (hero paragraph, Mission paragraph, catalog paragraph -- confirmed via repo-wide search to be the only three public occurrences anywhere in the codebase), replacing them with "produced through a certified pharmaceutical laboratory under rigorous manufacturing and quality-control standards" framing that avoids implying FDA/human-use/therapeutic approval. Rewrote the Mission statement on owner-provided wording (accessible pricing, a price-matching guarantee mentioned at the brand level only -- no mechanics invented, flagged to `PendingOwnerActions.md` instead), preserving the required closing sentence verbatim. Renamed the Mission's "Research-Grade Quality" feature to "Pharmaceutical-Grade Quality" with the owner's exact HPLC/mass-spectrometry line. All changes verified against real server-rendered HTML (hero `animate-float` class confirmed still present, zero GMP occurrences confirmed in the rendered page, both new image files confirmed serving via Next's image optimizer).

---

## Phase 6: Admin Portal Master Optimization + Finance & Expense Intelligence Sprint (started 2026-08-12)

**Status: merged to `master` and deployed to production 2026-08-12** — verified via production smoke test (homepage, catalog navigation, RUO signup gate, existing sign-in) after merge.

Audited the entire admin portal before building anything (read-only `Explore` pass): found there was no shared admin navigation at all -- every `/admin/**` page hand-coded its own header link row, none matching each other, with several real pages (Promotions, every Settings sub-page) reachable only by drilling through another page's own links, not from the dashboard. Also found two real, previously-unnoticed gaps: `/api/checkout` never checked a product's `pricingStatus`, so an archived product could still be fully checked out via a stale cart or direct API call regardless of storefront browse-side filtering; and the admin's own manual-invoice product picker filtered only on the legacy `inStock` flag, letting an admin hand-select an archived product onto a new invoice. Both fixed as part of the same pass that corrected Botulinum Toxin's name to "Botulinum Toxin Type A" and re-archived it (pharmaceutical-lab availability notice) -- historical invoices/orders/pricing/inventory records untouched, logged via a new `AdminAuditLog` entry.

Built `Admin -> Catalog -> Product Master` (`lib/adminProductMaster.ts`, `components/admin/ProductMasterTable.tsx`) -- reused the existing `listInventoryOverview()` query (same Product/pricing/inventory models the Inventory page already reads, not a second catalog database), enriched with archive status, case/individual-vial enablement, image status, SPA-invariant violation flagging, pricing-source classification, and last price/inventory-update timestamps (derived via `ProductPriceChange`/`InventoryLedgerEntry` groupBy queries). Every inline action (archive/reactivate, toggle Individual Vial public) reuses the existing `/api/admin/inventory/[id]/pricing` PATCH route rather than adding new write paths. Built a real shared `AdminNav` (`components/admin/AdminNav.tsx`, wired into `app/admin/layout.tsx`) implementing the target Overview/Sales/Customers/Catalog/Finance/Settings information architecture, then removed the admin dashboard's now-fully-redundant hand-rolled link row once every one of its links was reachable from the new shared nav.

Built the Finance & Expense Intelligence module from scratch: new `FinanceExpense` (13-category taxonomy, 6-value accounting-treatment classification, receipt reference, provider-reference idempotency key) and `InventoryPurchase` (COGS-feeding stock purchases, deliberately separate from `FinanceExpense` so a resale purchase is never double-counted as both COGS and a generic expense) Prisma models, plus a new `InvoiceItem.costOfGoods` column (nullable, never backfilled -- a null cost is reported as "unknown," never fabricated as $0). Discounts/credits, refunds, and inventory loss are deliberately **not** modeled as new tables -- `lib/finance/reports.ts` reads them directly from the existing `InvoiceDiscount`/`InvoiceRefund`/`InventoryLedgerEntry` (DAMAGE_LOSS) records, so a discount or refund can never drift into a second, independently-maintained ledger. Built `Admin -> Finance` (tabbed Dashboard/Expense Ledger/Discounts & Credits/Inventory-COGS/Refunds/Vendors, with This Month/Last Month/Quarter/Year/Custom range filtering) and a real server-generated multi-sheet Excel export plus a CSV export (extended the existing `lib/export.ts` rather than writing a second exporter). Wired real shipping-cost automation into the invoice label-purchase flow (`lib/fulfillment/labels.ts`) using Shippo's own transaction id as an idempotency key -- dormant today only because label *purchasing* itself is still gated behind Trust & Safety account verification (pre-existing, unrelated gate), and goes live automatically the moment that's lifted, with zero further code change. Added a "Record Shipping Expense" link on the invoice Shipments panel for the real current manual (Pirate Ship) workflow, deep-linking into Finance with the category and invoice pre-filled.

Closed the invoice hard-delete gap the audit found: `permanentlyDeleteInvoice()` previously only checked "is this already in Trash," then ran a plain cascading `prisma.invoice.delete()` with no check for real linked history. `lib/invoices/deletionEligibility.ts` now blocks deletion (single or bulk) whenever an invoice has payments, refunds, a linked storefront order, shipments, inventory movement, a redeemed promotion code, or linked Finance records -- enforced at the data layer (the API refuses it regardless of UI state), with the Trash UI showing exactly why an invoice is blocked and requiring a typed `DELETE N INVOICES` confirmation phrase for bulk deletion.

Test coverage added for the pure decision logic introduced this phase (33 new tests): `resolveFinanceRange`'s month/quarter/year/custom-range boundary math (including a real UTC-parsing timezone bug caught and fixed by the test itself), `computeInvoiceDeletionEligibility`'s eight block conditions individually and in combination (the bulk mixed-eligibility case), Product Master's SPA-invariant/missing-price/pricing-source derivations, and inventory-loss cost-basis calculation (never retail value, never a fabricated $0 for a product with no recorded supplier cost). The existing repo-wide `adminAuthCoverage.test.ts` guard automatically extended its coverage to every new `/api/admin/finance/**` route and the new bulk-delete route, confirming non-admin requests are refused with zero additional test-writing needed.

**Pre-signup RUO/21+ gate (2026-08-12, merged same day)**: new account creation now requires passing the RUO/21+ agreement before Clerk ever collects signup credentials -- enforced server-side at the single Next.js sign-up route (`app/sign-up/[[...sign-up]]`) rather than intercepting the Client Sign In modal client-side, so every entry point (the header modal's own "Sign up" link, the dedicated `/sign-in` page's link, and a direct URL visit) goes through the same check with no separate route to bypass. A visitor with no valid pending acceptance sees `RuoSignupGate` instead of Clerk's `<SignUp/>`; accepting POSTs to a new endpoint that server-validates both required checkboxes, records the acceptance against an opaque short-lived token (no email exists yet -- there's no Clerk session), and sets an HttpOnly cookie. Once signup completes and the user lands on `/account`, the existing self-service identity-resolution flow links that pending acceptance to the real `User` row, idempotently. Reused the existing versioned RUO architecture throughout (`lib/compliance/ruo.ts`, `ComplianceAcknowledgment`) rather than building a second disclaimer system, and extracted the shared checkbox/copy UI so the pre-checkout modal and the new gate can never drift apart. While auditing this module, found and fixed a real pre-existing bug: `ComplianceAcknowledgment.userId` is a foreign key to the internal `User.id`, but every existing call site (checkout, the standalone accept route, the status route) was passing Clerk's raw session id instead -- a signed-in customer's checkout would have thrown an unhandled foreign-key violation at the RUO-recording step, the first time any real signed-in customer completed a purchase. Never surfaced before now because the app is pre-launch (zero real orders). Verified against the live database via a disposable rehearsal script (12/12 checks) and, post-merge, against production itself via a full curl-based request sequence (unauthenticated gate render, bad-input rejection, valid-acceptance cookie issuance, gate lifting, forged-cookie rejection, direct-URL variants) -- the Chrome browser extension was not connected this session, so a live mouse-driven click-through and mobile visual check were not possible and are recommended as a follow-up before broad announcement.

**Admin master price table + Single Vial terminology update (2026-08-13, merged same day)**: Product Master's pricing columns rebuilt to the owner's specified layout -- default alphabetical sort, sticky Archive/Singles checkbox columns immediately left of Product, then Cost | Storefront Case | SPA | Single Vial | 5% | 8% | 10% | 15%, with the prior operational columns (category, source, backorder, image, inventory, updated) retained after. Bulk tiers are computed fresh from Storefront Case via a new pure `calculateBulkTierPrices()` (never SPA, never independently stored, so a case-price edit can never leave a stale bulk column). Single Vial price is now shown to admin regardless of whether public Singles sales are enabled -- previously it was hidden entirely once Singles was off, a real visibility gap for a field the owner needs to audit either way. Added a genuine server-side invariant (`SinglesRequiresPriceError`, same throw-and-roll-back pattern as the existing SPA invariant): enabling public Singles sales with no valid stored price is rejected rather than silently publishing an unpriced option, verified against the live database via a disposable rehearsal script (6/6 checks). Globally renamed "Individual Vial" to "Single Vial" across every rendered customer/admin string (product selector, cart toast, checkout line items, admin pricing panel, invoice correction dialog, pricing report email) via the shared label maps that already fan out to every consumer -- internal field/enum names deliberately left unchanged, matching the owner's explicit instruction not to rename stable internals for a copy change. While verifying the deploy, a bare-curl request to every Clerk-authenticated route (`/admin`, `/account`) briefly appeared to return 404 in production, including on deployments from hours earlier that had already been verified working -- traced conclusively (not a code regression: no runtime errors logged, no `middleware.ts`/`proxy.ts` change, identical behavior on old and new deployments alike) to Clerk's development-instance cross-domain "dev browser" handshake, which real browsers complete automatically but bare curl does not unless the full redirect chain and browser-realistic headers are used. Logged as a real, owner-actionable item (confirm Clerk key mode before wide traffic) rather than either ignored or wrongly treated as a code incident.

**MOTS-c pricing verification, predictive search, catalog gold consistency, and visual case-study archive (2026-08-14, merged same day)**: verified MOTS-c has exactly two live strengths (10mg/40mg, no stray 20mg anywhere in the database, seed data, or code) and confirmed 40mg pricing ($849 standard/$665 SPA/$92 individual) and 10mg pricing ($350/$275/$39) both match the approved source of truth already in production, with no correction needed. Replaced the navbar's plain search-on-submit with a real predictive/autocomplete dropdown (`components/storefront/PredictiveSearch.tsx`) reusing the existing tiered `rankSearch()` matcher against a lightweight, lazily-loaded product index (`GET /api/storefront/search-index`) rather than a new per-keystroke database query or a duplicated matching algorithm. Carried the homepage Catalog Directory's metallic gold-gradient frame treatment into the full `/categories` page's cards (same gradient token, specular-highlight technique, and gold divider rule), leaving `lib/storefront/categoryAccents.ts`'s alternating icon colors completely untouched per explicit instruction. Established this project's first visual-documentation archive (`docs/case-studies/pepscore/{before,after,assets,screenshots}/`) and the underlying cross-project standard it follows (`AO-AI-Solutions/docs/ProductStrategy.md`, "AOAI baseline delivery standard") — preserved the pre-v2 hero/Mission images (`hero-vials.jpeg`, `hero-vials-new.png`, `mission-vials.png`) as BEFORE assets and the current hero/Mission/representative product photography as CURRENT assets. While auditing image references for this work, found a real, separate gap: `app/layout.tsx`'s Open Graph metadata still points at the original `hero-vials.jpeg`, unchanged since the hero photo itself moved on to `pepscore-hero-v2.png` two revision passes ago — logged rather than fixed, since it was outside this sprint's scope. Landing-page and live-application screenshots remain **pending**, not fabricated: the Chrome browser extension was not connected at any point this session (same limitation recorded in the RUO-gate entry above), so `docs/case-studies/pepscore/screenshots/` is currently empty with the exact follow-up captures needed documented in its manifest.

---

## Phase 7: RBAC Migration, Storefront Finalization, Catalog Taxonomy & Asset Standardization (2026-08-14 – 2026-08-17)

**Status: merged to `master`, deployed to production.** This phase closed out the homepage-refresh preview branch's remaining commerce-correctness gaps, replaced the project's admin-authorization model wholesale, and re-grounded the entire product catalog's imagery and category language in owner-approved source material rather than accumulated prototype artifacts.

**Storefront correctness and merchandising**: item-level invoice refunds (`668f2be`) extended the existing manual `InvoiceRefund` obligation model (never a live payment-processor refund API — refunds remain admin-recorded and completed manually, exactly as documented in Phase 3) with a nullable `invoiceItemId` plus a frozen snapshot (`grossItemValue`, `allocatedDiscount`, `effectivePaidValue`, `refundedQuantity`) computed once at request time from that item's historical total and its share of any invoice-level discount — never recomputed from current catalog pricing or a later-reversed discount. The new `lib/invoice/refundAllocation.ts` implements proportional discount allocation by each item's share of the items total (verified against the owner's own worked examples — 2×$100 with a $50 discount splits $75/$75; $100+$300 with a $100 discount splits $75/$225 — with penny-accurate rounding reconciliation), and closed two genuine pre-existing gaps found mid-audit: the original whole-invoice `requestRefund()` had no cap against `amountPaid` at all, and `completeRefund()` could let two near-simultaneous pending refunds jointly overrun a real remaining balance. Both gaps were fixed with the same commit, not deferred. Verified via 16 new pure-function unit tests plus a disposable real-Postgres rehearsal script exercising quantity-partial refunds, prior-partial-refund capping, concurrent-duplicate-request idempotency (`Promise.allSettled`), and multi-item combined-budget enforcement — 15/15 checks passed.

A run of smaller storefront fixes followed the same audit-before-building discipline: the storefront card visual system was finalized across all three homepage sections (Light Champagne, Dark Bronze, and a new flat "Dog Tag Titanium" metallic treatment for Bulk Pricing) and one genuine defect was caught by direct `getComputedStyle()` inspection of the *live* production page, not source review alone — the Explore Catalog cards were gold-**bordered**, not gold-**filled**, because an earlier gold-consistency pass had put the gradient on the outer `<Link>` wrapper while an inner `<div>`, inset by 1px and covering the entire visible surface, still carried the old near-black fill (`6119d0c`). Product Master gained an admin-controlled `Product.merchandisingStatus` (NONE/POPULAR/BEST_SELLER, family-scoped) replacing a hardcoded per-name badge list (`fecc765`). Nine footer "Products" links that all pointed at the same generic `/#products` anchor now deep-link to each product's real detail page (`6ae14b3`). And `ProductDetail.tsx` — the standalone product page, as distinct from the catalog-grid `ProductCard` — was found to never have implemented a sell-unit selector at all: Add to Cart always hardcoded `CASE_STANDARD`, so a customer landing directly on a product's own page could not buy the Single Vial option the same product's catalog card advertised as available (`65e63e1`). Fixing it also surfaced and corrected a real eligibility-data error: every strength of five product families (Semaglutide, Tirzepatide, Retatrutide, NAD+, Glutathione) had `individualSalesEnabled=true`, when the owner had only approved one strength per family for Single Vial sale — corrected via the real pricing service for full audit-trail parity with a normal admin edit.

### Mini Case Study: RBAC Migration — Centralizing Admin Authorization

**PROBLEM**: admin authorization had accreted into three different, independently-drifting shapes across the codebase — a per-route inline function (`function isAdmin(userId) { return userId === process.env.ADMIN_CLERK_USER_ID }`) duplicated across 67 separate API route files, a shared-but-thin `lib/isAdmin.ts` consumed by 9 admin/account pages, and a third inline redirect-on-mismatch pattern duplicated across 18 more admin pages. All three ultimately compared a request's Clerk user id against one hardcoded environment variable — a pattern the original Phase 2G roadmap had already flagged as "fine for one operator, explicitly needing real roles once there's more than one" (see Section 5, "Auth").

**REQUIREMENTS**: one authorization surface, callable identically from API routes and pages; role state that lives in the database rather than solely an environment variable, so a second admin can eventually be added without redeploying; and zero regression risk against the existing `adminAuthCoverage.test.ts` regression guard that scans every `app/api/admin/**` route for a real auth check.

**ARCHITECTURAL DECISION**: build `lib/auth/rbac.ts` around a `User.role` database column (`Role` enum, `ADMIN`/implicit customer), with `ADMIN_CLERK_USER_ID` demoted from "the mechanism" to a one-time bootstrap: the first authorized request from that exact legacy Clerk identity promotes or creates its `User` row to `ADMIN` via an upsert, after which every future check for that identity reads the database like any other admin. Two functions cover every call shape: `requireAdmin()` (returns the Clerk user id or `null`, for API routes) and `isCurrentUserAdmin()` (a non-throwing boolean, for pages and client-facing "am I admin" checks like `/api/admin/whoami`).

**IMPLEMENTATION** (`7e03940`): every one of the 67 inline route checks, the 9 `lib/isAdmin.ts` call sites, and the 18 page-level redirect patterns were migrated onto the two new functions; `lib/isAdmin.ts` and its own test were deleted as fully superseded rather than left as a second, now-redundant path.

**FAILURE/EDGE CASE DISCOVERED**: the existing `adminAuthCoverage.test.ts` regression guard — which greps every admin API route for a real auth check — was itself written against the *old* pattern (a literal `process.env.ADMIN_CLERK_USER_ID` comparison). Migrating the routes without also migrating the guard would have left it either false-failing on every correctly-migrated route or, worse, silently passing routes that no longer had any real check at all.

**ROOT CAUSE**: a regression guard that pattern-matches source text is only as good as the pattern it's written against; changing the underlying implementation without updating the guard turns it from a safety net into a false signal.

**FIX**: `adminAuthCoverage.test.ts` was rewritten to enforce the *new* contract — import `requireAdmin`, call it, gate on a falsy result, and contain no leftover inline env-var comparison — plus two explicit additional checks for the admin-only routes that live outside `app/api/admin` entirely (shipping label purchase, the CPA export route), which the original grep-by-directory approach would have missed by construction. A parallel `adminPageAuthCoverage.test.ts` was added three days later (`0cbd866`) specifically because the original coverage test had no page-level equivalent — a gap this session's own admin predictive-search page would otherwise have shipped through unguarded.

**TEST/VERIFICATION**: the full suite passed with the rewritten guard now actively enforcing the new pattern across all 67+9+18 migrated call sites, plus the two additional non-`/admin` routes it was extended to cover.

**PRODUCTION RESULT**: exactly one authorization implementation exists in the codebase today (`lib/auth/rbac.ts`), role state is database-backed rather than solely an environment variable, and the regression guard that enforces "every admin route has a real check" was upgraded in the same change rather than left stale — closing a real security-regression risk window that a naive migration would have opened.

The RBAC migration's admin-authorization foundation was reused immediately: **admin predictive customer search** (`0cbd866`) wraps the existing `listCustomers({ search })` query (already flexible-phone-format-matching) in a new admin-gated API route returning only the minimal disambiguation fields, never a full customer record — and deliberately does not preload-and-filter client-side, since bulk-fetching customer PII to the browser is a real privacy boundary this project has held consistently since the Customer Portal's own authorization design. The same commit generalized `PredictiveSearch` with optional `leftSlot`/`rightSlot` props so the homepage search bar could reuse the same debounced, keyboard-navigable combobox rather than a second, submit-only implementation — a decision that mattered directly two commits later (see the Mobile Navigation mini case study below).

**Catalog taxonomy and regulatory-risk language cleanup** (`fc1a182`, `811632d`): six merchandising-category labels that still carried personal-outcome-implying words despite being suffixed "Research" (Anti-Aging, Fat Loss, Sexual Health, Cosmetic, Performance, Injury) were retitled to match the already-clean tone of their own description text, mirrored into the homepage `CatalogDirectory` tiles. A dedicated script (`scripts/fix-high-risk-product-descriptions.ts`) corrected 17 live `Product.description` values still containing personal-outcome language (fat loss, cosmetic, anti-aging, libido, tanning, wrinkle reduction, body contouring, sexual wellness) to neutral, RUO-compatible research-mechanism language — run against production with `--apply`, then re-verified that zero risky terms remained anywhere in the live catalog. A separate raw-category fix (`scripts/fix-snap8-category.ts`) corrected Snap-8's underlying `Product.category` from "Cosmetic Peptide" to "Neuromuscular Peptide," matching both the taxonomy's own "[Mechanism] Peptide" naming convention and the product's actual description. Both scripts follow the same dry-run-by-default pattern already established for this class of production data-correction script.

### Mini Case Study: Asset/Catalog Standardization — Family-Image Resolver and Print-Production Archive

**PROBLEM**: product imagery had accumulated across multiple prototype rounds (flat-text-compositing family photos, pre-approval label previews, a pre-Next.js static HTML prototype) with no single canonical mapping from a live catalog product to an approved image, print labels stored inside `public/` where they were unintentionally publicly servable, and no durable record of which asset had actually been owner-approved versus rejected along the way.

**REQUIREMENTS**: every one of the 68 live distinct product names must resolve to exactly one owner-approved image; sibling strengths of the same product family must intentionally share one photo (photography is family-level, never per-strength, by explicit design — none of the approved images carry mg/strength text); print-production files (labels, manifests) must not be publicly servable just because earlier prototype work happened to place them under `public/`; and every discarded prototype must be preserved as before/after evidence rather than deleted outright.

**ARCHITECTURAL DECISION**: one shared resolver module, `lib/storefront/productImages.ts`, consumed by both the homepage catalog grid and individual product detail pages, mapping full product **name** (not per-strength SKU) to one of 64 owner-approved family images at `public/images/products/families/`; everything print-production-related (labels, manifests, audit trail) moved to `documents/labels/` and `docs/assets/`, deliberately outside `public/`.

**IMPLEMENTATION** (`2d9bd66`, `102d207`): imported the two owner-approved packages (`Pepscore_Storefront_Family_Images_APPROVED_CORRECTED.zip`, `Pepscore_Print_Labels_APPROVED_CORRECTED.zip`) and their manifests, preserved for provenance at `docs/assets/manifests/`. 117 owner-approved print labels (107 3mL, 10 10mL, both PNG and PDF) landed at `documents/labels/{3ml,10ml}/`. 55 old per-product images moved to `public/images/products/legacy/` as before-state evidence; three rounds of rejected flat-text-compositing prototypes moved to `scripts/assets/_rejected-prototypes/` (never deleted). The pre-Next.js static HTML prototype was relocated to `docs/case-studies/pepscore/legacy-prototype/` after confirming zero remaining references to its obsolete "Holistic" branding anywhere in the live app. A dedicated identity finding was resolved without touching transactional history: the archived legacy database record named "BPC 10mg + GHK-Cu 50mg + TB500 10mg" and the active "GLOW70" record are the *same physical blend*, recorded under its old spelled-out name before the GLOW70 brand existed — both now resolve to the same `GLOW70.png` at the image-resolution layer, but the two database rows were deliberately never merged or deleted, since doing so could affect pricing, order, or inventory history that the image-mapping fix has no business touching.

**FAILURE/EDGE CASE DISCOVERED**: a real `next/image` bug found during verification — the optimizer silently fails (`naturalWidth 0`, no console error) for any static path containing a space-surrounded `+`, which broke three of the approved filenames (`CJC-1295 + Ipamorelin`, `Cagrilintide + Semaglutide`, `BPC 157 + TB500`) even though the raw static file served correctly at the same URL when fetched directly. A second finding: a Semaglutide image swap appeared not to take effect after deployment — traced to `.next`'s image-optimization cache serving stale pre-swap bytes from a fresh browser tab, resolved by clearing the cache and confirmed via SHA-256 hash comparison of source versus served bytes (documented in `docs/assets/audits/2026-08-17-SEMAGLUTIDE-DEFERRED.md`).

**ROOT CAUSE**: the `+` bug is a Next.js image-optimizer URL-encoding edge case specific to a space-surrounded `+` character (confirmed by finding `NAD+.png`, which has no surrounding spaces around its `+`, was unaffected — isolating the trigger to the space-plus-space sequence, not the character alone); the caching issue is a standard CDN/browser image-cache staleness problem that a same-name file overwrite doesn't reliably defeat.

**FIX**: renamed all three affected files to use `"and"` in place of `"+"` and updated every reference; subsequent image swaps in the same sprint (the hero and social-preview images) were deliberately saved under new *versioned* filenames rather than overwriting the old name, specifically to defeat any CDN/browser cache of the old filename rather than trust propagation.

**TEST/VERIFICATION**: a full production data-integrity audit (folded into the later soft-launch readiness sprint, `2d44e37`) confirmed all 120 catalog products have zero missing or broken images — every one of the 51 currently-active product names has a curated, owner-approved family image, with the two-layer resolver verified to have complete coverage.

**PRODUCTION RESULT**: one canonical image-resolution layer replaces what had been an ad hoc, prototype-accumulated mapping; print-production assets are no longer publicly servable; and a full, hash-verified provenance trail (`docs/assets/audits/`) exists for every asset decision, including the ones explicitly rejected.

### Mini Case Study: Mobile Navigation Event-Race Defect

**PROBLEM**: reported by direct owner observation — every item in the mobile hamburger menu (Products, Categories, Bulk Orders, Why Us, Mission, Contact, Client Sign In / Create Account) was dead on first real tap.

**REQUIREMENTS**: every mobile nav link must be tappable on the first real touch interaction, with no regression to the desktop search or the existing "outside click closes the dropdown" behavior of the storefront predictive search box.

**ARCHITECTURAL DECISION**: rather than patch the symptom (e.g., re-ordering DOM elements or adding a delay), trace the actual event sequence and correct the ownership boundary of the outside-click handler.

**IMPLEMENTATION / ROOT-CAUSE ANALYSIS** (`0c63e7b`): `PredictiveSearch`'s document-level `mousedown` outside-click listener was wired, via its single `onClose` callback, to close the *entire surrounding mobile menu* — not just its own small search dropdown. A tap on any nav link below the search box (outside `PredictiveSearch`'s own `containerRef`, but still inside the open mobile menu) fired that document-level listener on `mousedown`, which unmounted the whole menu — the tapped link included — *before* the browser's subsequent `click` event could ever reach it. This is a textbook event-race defect: two DOM events (`mousedown`, then `click`) fire in sequence for a single tap, and the first one destroyed the element the second one needed to still exist.

**FIX**: split the single `onClose` callback into two distinct callbacks with different semantics — `onClose` (search-field-only collapse, its original, narrower behavior preserved) and a new `onSelect` (fires only on an actual result pick or search submit, never from an incidental outside click). `Header.tsx`'s mobile search instance now closes the menu via `onSelect` instead of `onClose`, and gained its own correctly-scoped outside-click handler (a ref on `<header>`, using the same `containerRef.contains()` pattern already established in `AdminCustomerSearch.tsx`) so a genuine outside tap still closes the menu — just without destroying the menu's own internal links in the process. `aria-expanded`/`aria-controls` were added to the hamburger toggle button as a related accessibility correction. A second, load-bearing fix: the nav item list had been duplicated verbatim between the desktop `<ul>` and the mobile menu — extracted to one shared `lib/storefront/navItems.ts` source so the two surfaces can no longer silently drift apart.

**TEST/VERIFICATION**: this defect could not be reproduced with a plain synthetic `click()` call — the regression test (`components/storefront/PredictiveSearch.test.tsx`) uses real `user-event` `mousedown`/`click` sequences to actually reproduce the browser's real event ordering, and was deliberately confirmed to **fail** against the old `onClose`-only wiring before being restored to verify the fix. `lib/storefront/navItems.test.ts` independently validates every hash `href` against a real section id read from source. Because this repository's existing Vitest setup ran in a Node environment with no DOM (fine for the pure-business-logic tests that had driven every prior test addition), `jsdom` and `@testing-library/react`/`user-event`/`jest-dom` were added as dev dependencies specifically to make real event-timing tests possible at all — a testing-infrastructure investment triggered directly by this one defect class.

**PRODUCTION RESULT**: every mobile nav link is tappable on first touch; the regression test guards specifically against a callback-scope reintroduction of this exact bug, not just "the menu closes somehow"; and the shared `navItems.ts` source removes the parallel-maintenance risk that could otherwise reintroduce a similar drift between desktop and mobile independently of this specific event-handling fix.

---

## Phase 8: Pepscore Intelligence — AI Architecture, Retrieval, and Live-Model Integration (2026-08-18)

**Status: merged to `master`, deployed to production; public customer-facing activation remains off.** This is the largest single body of engineering work in the gap this backfill covers — 27 commits (`47321e6` through `2d5ccb4`) building a complete, provider-agnostic AI subsystem from a zero-dependency foundation through a live, credentialed, retrieval-grounded generation pipeline, entirely dark-deployed behind independent kill switches at every stage. No commit in this sequence enabled public customer-facing AI at any point.

**AI-0B — the foundation** (`47321e6`): built before any customer-facing surface existed, and verified by direct `grep` to have zero files outside `lib/ai/` importing from it — meaning the storefront, admin, checkout, auth, portal, invoice, shipping, and email code paths were architecturally incapable of being affected by this work regardless of the feature flag's state. The foundation has six layers: a provider abstraction (`lib/ai/providers/` — an `AiProvider` interface, a thin `AiGatewayProvider` fetch wrapper against Vercel AI Gateway's REST endpoint with no SDK dependency, a `ProviderRouter` implementing primary→fallback→safe-failure, and a `MockAiProvider` used by every test in the entire AI subsystem until a real credential existed); a nine-category rule-based-first request classifier and a centralized ALLOW/REFUSE/ESCALATE policy engine (`lib/ai/policy/`) with independent PASS/REWRITE/REFUSE/ESCALATE output validation, where `UNKNOWN` and sub-70%-confidence classifications always fail closed to `ESCALATE`, never `ALLOW`; a role mapping (`lib/ai/permissions/roles.ts`) onto the existing `lib/auth/rbac.ts` foundation rather than a parallel permission system; structured, non-prompt-capturing audit models (`AiComplianceEvent`, `AiUsageEvent`) following the exact `AdminAuditLog`/`CustomerAccessEvent` convention already established elsewhere in the schema; a tiered retrieval and citation system (`lib/ai/retrieval/`, `lib/ai/citations/`) where Tier 1 wraps the existing `lib/storefront/searchRank.ts` matcher directly rather than duplicating it, and Tiers 2/3 are empty in-memory fixtures — not a database table — since pgvector and any curated-content model were explicitly deferred until real curated content exists to justify them; and a prompt-injection defense (`lib/ai/security/retrievalSanitizer.ts`) that treats every retrieved source as untrusted data, never instructions, scanning it for injection patterns before it can enter a generation context. `runAiPipeline()` composes every layer into one fail-closed request flow. A 34-test consolidated safety matrix (`lib/ai/__tests__/safetyMatrix.test.ts`) exercises ALLOWED, REFUSED, JAILBREAK, OBFUSCATED HUMAN-USE, PROMPT INJECTION, ROLE/PERMISSION, CROSS-ACCOUNT ACCESS, ADMIN-DATA LEAKAGE, HIDDEN-INVENTORY LEAKAGE, OUTPUT VALIDATION, PROVIDER FAILURE, FALLBACK, RATE LIMIT, COST METERING, and CITATION INTEGRITY cases in one place. The schema addition was purely additive (80 insertions, 0 deletions, no existing model touched).

**AI-1.1 / AI-1.2 — first-party engagement analytics** (`1eee510`, `4b8bbb0`, `6a057fa`): reconciled a real gap the AI-intelligence direction depended on — search and product-engagement events had only ever gone to `@vercel/analytics`, a third-party hosted dashboard with no first-party queryable record. New `SearchEvent` and `ProductEngagementEvent` models, logged alongside (not instead of) the existing third-party `trackEvent()` calls, feed two new admin-only pages (`/admin/intelligence/search`, `/admin/intelligence/products`) doing plain aggregation — zero-result query tracking, top-query ranking, view/add-to-cart demand ranking — with an explicit design rule against fabrication: a product with adds-to-cart but zero recorded views is excluded from a rate calculation entirely rather than shown with a divide-by-zero artifact, and insufficient event history returns an empty array rather than a guessed value. This was deliberately the lowest-risk possible starting slice per the owner's own sequencing guidance: admin-facing only, first-party data only, no LLM call, no public exposure, no policy-engine involvement needed since it's pure aggregation.

**AI-1.3 / AI-1.5 / AI-1.6 — structured, retrieval-only intelligence (no model required)** (`a788daa`, `b23d002`, `943c8d7`): with `modelRoutes.ts` still empty and no approved model route to generate from, these three capabilities — structured compound comparison, category discovery, and a single-compound research-area explainer — are deliberately built to need no generation at all: pure Tier 1 catalog-plus-taxonomy lookup with full citations, taking structured input (explicit product names, a category slug) rather than a free-text box, matching two of the owner's own approved examples exactly ("Compare the research classifications of MOTS-c and NAD+," "What products are categorized under cellular aging research?"). Testing AI-1.3 exposed a real classifier gap: the system-generated request text these modules construct internally ("Compare the research classifications of...") matched no existing policy rule and fell through to `UNKNOWN → ESCALATE` on every call, even with zero personal intent present — fixed by adding narrow `CATALOG` rules for exactly these system-generated shapes, with an explicit test confirming `HUMAN_USE` rules still take priority whenever personal intent is present in the same request. AI-1.7 (`23d108f`) then proved the shared policy gate holds even when the "structured" product-name argument is itself attacker-controlled — a jailbreak or personal-dosing payload passed as a compare/explain product-name argument is refused before the database is ever touched, not silently treated as a literal string because it arrived through a structured field rather than free text.

**AI-1.4 / AI-1.9 — the Admin AI Control Panel** (`a57149a`, `3d20afa`): a read-only pre-activation observability dashboard (`/admin/intelligence/ai-status`) surfacing feature-flag state, provider/model config presence (booleans only, never secret values), approved-route count, rate limits, cost ceiling, corpus size, usage/compliance aggregation, and an unreviewed-`ESCALATE` safety review queue — kept deliberately read-only at first checkpoint, then given a real `markComplianceEventReviewed()` mark-reviewed action once the gap-audit flagged that a compliance queue nothing can ever clear isn't a real review capability, just visibility.

**AI-1.8 — the customer-facing entry point** (`03dce33`): closed a real gap found during roadmap reconciliation — AI-1.3/1.6 had built and deployed a tested compare/discover/explain API, but nothing anywhere actually called it, so no customer could reach it even with the feature flag on. `app/research/page.tsx` (`notFound()` when the flag is off, matching the API's own 503 posture) and `PepscoreIntelligenceConcierge.tsx` (three structured tabs, not a free-text box, matching the backend's own no-arbitrary-parsing posture) are that missing entry point — deliberately not linked into `Header.tsx`/`navItems.ts` in this pass, since that file was the exact site of the mobile-navigation regression fixed one phase earlier and a nav link isn't required for the route to function or stay dark.

**AI-1.10 / AI-1.11 — closing the "built but not wired" gap**: this session's own roadmap reconciliation twice found real infrastructure that had been built and independently unit-tested but never actually connected end-to-end. AI-1.10 (`8325558`) found that `retrieveForRole()` and `sanitizeRetrievedContent()` — both built and tested since AI-0B.4 — had no integration point anywhere inside `runAiPipeline()`; generation would have run with zero retrieved context and produced zero citations despite the citation schema existing from the start. The fix, `buildRetrievalContext()`, sanitizes every retrieved source before it can enter a prompt and drops (never partially includes) any source flagged `JAILBREAK`/`PROMPT_INJECTION`; retrieval now runs strictly after the input policy gate passes, never for a refused or escalated request. AI-1.11 (`3c719c0`) found a deeper structural problem underneath the same symptom: `AiGatewayProvider.complete()` always read `config.primaryModel` internally, meaning two provider instances built from the same config could never actually target two different models — a "fallback provider" was structurally impossible to construct as a genuinely distinct target. `buildProviderRouterFromConfig()` fixed both the wiring gap and the underlying construction bug together, returning `null` whenever a real router can't legitimately be built (flag off, no credential, no approved model route) rather than a router that silently can't reach a distinct fallback.

### Mini Case Study: Vercel AI Infrastructure Migration

**PROBLEM**: the AI-0B architecture had been built to require, before any live model call, that each specific model route be individually verified for Zero Data Retention (ZDR) eligibility and recorded in a static, code-enforced approval table (`lib/ai/providers/modelRoutes.ts`) — but the Vercel project this codebase was deployed under (originally under a personal/Hobby-tier team, `compscigrads-projects`) had no ZDR eligibility available on Vercel AI Gateway at all under that plan tier, independent of any application code.

**REQUIREMENTS**: a live model call must never be technically possible until a real, ZDR-eligible model route exists and a real Gateway credential is present in the correct environment — and every diagnostic step taken to confirm credential presence during the transition must never print, log, or expose the credential's actual value.

**ARCHITECTURAL DECISION / MIGRATION**: the owner transferred the Pepscore Vercel project from the personal Hobby-tier team to the AOAI team's Pro-tier project, whose plan supports ZDR on Vercel AI Gateway. This is a project/infrastructure-level change, not a code change — but it directly gated whether any of the AI-0B/AI-1 code built up to this point could ever make a real network call.

**IMPLEMENTATION**: over three separate rounds, the owner added or replaced the `AI_GATEWAY_API_KEY` credential in the Vercel dashboard, and each time this session verified presence with a **boolean-only** diagnostic — a single log line at the top of the one already-public route that reads AI config, printed and read via Vercel's own runtime logs, then reverted in the very next commit once confirmed (`671a6db`/reverted same slice; `f256f2f`/`7fff7fc`; `e24e374`/`260b679`). At no point did any diagnostic print, transmit, or persist the credential's actual value — only its presence as a boolean. Round one (`671a6db`, `d0c53a1`) confirmed the reported key was *not* actually present in the Vercel production environment despite being reported as available, and the AI-1.12 commit that followed built the entire live-model-integration wiring anyway, engineered to fail closed to `NOT_CONFIGURED` with zero network calls until the credential existed — the same MockAiProvider-driven proof pattern used for every other AI-0B/AI-1 slice all session. Round two (`f256f2f`/`7fff7fc`) confirmed a second, directly-pasted key was genuinely present. Round three (`e24e374`) followed the actual AOAI project transfer — the owner replaced the credential with a new AOAI-scoped key — but this round's diagnostic was superseded by a more conclusive signal: the AOAI AI Gateway dashboard's own key-usage timestamps showed Production was still authenticating with the **pre-transfer Hobby-team key**, not the newly-pasted AOAI key, revealing a **team-scoped Gateway credential issue** — the Vercel project's runtime was still resolving the old team's credential rather than the new one, a discrepancy no application-code check alone could have surfaced (`260b679`).

**FAILURE/EDGE CASE DISCOVERED**: a credential can be genuinely present in a Vercel project's environment-variable list and still not be the credential actually used at request time, when a project transfer between teams doesn't cleanly migrate which team's Gateway account a running deployment authenticates against.

**FIX**: by the time of the next AI capability commit (`2d5ccb4`, AI-1.18), the live pipeline was confirmably reaching a real model in production — that commit's own live-verification finding describes retrieval returning zero results while the model still produced answers, which is only possible if a real model call was actually succeeding. The exact mechanical step that resolved the team-scoped credential mismatch between `260b679`'s finding and `2d5ccb4`'s live-verification session (a redeploy to force the runtime to re-resolve the correct team's credential, a dashboard-side correction, or something else) is **not captured in a commit or a doc** — this is a genuine documentation gap in this backfill, not a fabricated resolution step.

**TEST/VERIFICATION**: `MODEL_ROUTES` was populated with two real, genuinely distinct providers — `anthropic/claude-haiku-4.5` (primary) and `google/gemini-3.1-flash-lite` (fallback) — each individually verified against Vercel's own published ZDR provider list (`vercel.com/docs/ai-gateway/security-and-compliance/zdr`, accessed 2026-08-18) and confirmed to *not* be the one documented ZDR exception model (`anthropic/claude-fable-5`). Every live request additionally and independently sets `providerOptions.gateway.zeroDataRetention=true`, which Vercel enforces server-side per request — a request only routes to a currently-confirmed-ZDR provider or fails closed with a 400, regardless of what the static approval table claims, giving the system two independent layers of ZDR enforcement rather than one.

**PRODUCTION RESULT**: a real, credentialed, ZDR-verified live-model path exists and was exercised in production (see the following mini case study for what that live verification actually found) — but the exact step that resolved the team-scoped credential mismatch is an honest gap in this project's documentation record, not a claimed-and-unverified fact.

**AI-1.12 / AI-1.13 — live-model wiring and the last credential-blocked capability**: `AI_LIVE_MODEL_ENABLED` was split from `AI_FEATURE_ENABLED` in AI-1.15 (see below) specifically because live-verification work needed to happen without opening public customer access — but the underlying live-test capability (`app/api/admin/ai/live-test/route.ts`) was built in AI-1.12 (`d0c53a1`): admin-only, accepts only a fixed `promptKey` from a synthetic, non-PII prompt set (`lib/ai/testing/syntheticPrompts.ts`) matching the owner's exact allowed/prohibited examples — never free text — and runs through the identical `runAiPipeline()` every future generation capability will use. AI-1.13 (`4c45a0c`) then closed the one Pepscore Intelligence capability marked `BLOCKED — OWNER CREDENTIAL REQUIRED` since its very first mention in AI-1.3's own header comment: free-text research question answering, which genuinely needs a live model to parse arbitrary intent (unlike compare/discover/explain's fixed, system-generated request shapes). The engineering itself was never blocked, only live execution — `askResearchQuestion()` returns `UNAVAILABLE` with zero network calls when no live provider can be built, so this capability was built, tested, and dark-deployed the same way every other AI-1 slice was, regardless of credential state at the time.

**AI-1.15 — splitting the kill switch that was doing two jobs**: live admin verification surfaced a real design flaw — `buildProviderRouterFromConfig()`'s first check was `if (!config.featureEnabled) return null`, meaning `AI_FEATURE_ENABLED` (intended purely as the public-customer-AI gate) was *also* silently gating whether any live-model call could happen at all, including admin-only internal verification. One flag was doing two structurally different jobs, which made it impossible to verify a live model without also opening the public customer route. `8fd5299` split this into two independent flags: `AI_FEATURE_ENABLED` (public customer AI only — gates `/api/ai/intelligence`'s 503 and `/research`'s `notFound()`) and `AI_LIVE_MODEL_ENABLED` (the real spend kill switch — gates `buildProviderRouterFromConfig()` itself). The same commit fixed a separate, unrelated UI defect found during the same live-verification pass: native `<select>` dropdown contrast was unreadable site-wide, because a `<select>`'s expanded option-list popup is rendered by the browser/OS chrome, not by Tailwind background utilities on the element itself — every dark-themed select set `text-white` on the closed control but never told the browser to render the *popup* in dark chrome. AI-1.16 (`d90e47b`) found the first-round CSS fix (`color-scheme: dark`) did not reliably hold across real browser/OS combinations under actual manual testing, and replaced the two highest-visibility instances with a fully custom `DarkListbox` component owning its own rendering end to end, while strengthening the CSS-only fix as defense-in-depth for the ~28 other native selects not yet converted.

### Mini Case Study: AI First-Party Grounding Defect

**PROBLEM**: during live-model verification following the credential migration above, a natural-language research question ("What catalog families are associated with mitochondrial research?") produced an answer from the live model that *looked* correct and relevant — but Pepscore's catalog genuinely has an authoritative "Mitochondrial Peptide" category (MOTS-c, SS-31, Humanin), and closer inspection of the retrieval trace showed **zero results** had actually been retrieved from Pepscore's own catalog for that query. The model had answered from its own training-data knowledge, not from grounded Pepscore data — despite the entire AI-0B retrieval-and-citation architecture existing specifically to prevent exactly that.

**REQUIREMENTS**: every research-question answer the live pipeline produces must be genuinely grounded in real, retrieved Pepscore catalog data and carry real citations back to that data — an answer that happens to sound plausible from a model's general training knowledge is not an acceptable substitute, regardless of accuracy, because it can't be verified against Pepscore's own actual product line and breaks the citation-integrity guarantee the safety matrix already tests for.

**ARCHITECTURAL DECISION**: trace the actual retrieval call rather than assume the failure was in generation, prompting, or the model itself.

**FAILURE DISCOVERED / ROOT CAUSE**: `Tier1CatalogRetrieval` (`lib/ai/retrieval/tier1Catalog.ts`) wraps `rankSearch()` — the exact same matcher the storefront search box uses, deliberately reused rather than duplicated per the owner's own AI-0B instruction ("do not duplicate `searchRank.ts` merely to label it AI retrieval"). `rankSearch()`'s every-token-must-match rule is correct for its original purpose — a user typing a partial product name into a search box — but a natural-language question tokenizes into mostly stopwords ("What," "catalog," "families," "are," "associated," "with") that never match a product name or category field, so `rankSearch()` legitimately returns nothing for a phrasing shape it was never designed to handle, even when Pepscore has fully authoritative data for the underlying topic. This is a genuine **`rankSearch` mismatch** between the matcher's design intent (exact/near-exact product lookup) and its new caller's actual input shape (open-ended natural-language questions) — not a bug in `rankSearch()` itself, which continued behaving exactly as its own existing unit tests specify.

**FIX**: rather than loosen `rankSearch()` itself — which would risk regressing real customer search behavior it has served correctly since the predictive-search work — a second, adapter-local matching pass was added inside `Tier1CatalogRetrieval`, running only when `rankSearch()`'s stricter tiers find nothing at all: a product-name-mentioned-in-query check and a category-word-mentioned-in-query check (with a minimum word length to exclude generic tokens like bare unit abbreviations), both built only from real, already-loaded `Product`/taxonomy data — no shadow catalog, no hardcoded query text. This is a genuinely **research-specific retrieval adapter**, additive to the existing matcher rather than a replacement for it.

**TEST/VERIFICATION** (`2d5ccb4`): unit coverage was added for the new fallback path specifically, plus — critically — one **true end-to-end pipeline test** that wires the real `Tier1CatalogRetrieval` (not a stub or mock) through the actual `runAiPipeline()`, so that a future regression in either the adapter's matching logic or the pipeline's own context-injection step would be caught by this one test without requiring a live model call to detect it.

**PRODUCTION RESULT**: natural-language research questions that map to real Pepscore catalog data now retrieve that data and produce genuinely grounded answers with real citations, closing the gap between "the retrieval and citation architecture is built and unit-tested" (true since AI-0B) and "the retrieval and citation architecture actually fires for the kind of question a real customer would ask" (only true as of this fix) — the exact "built but not wired" failure class named directly in this project's own standing engineering discipline (see Section 13, "Lessons Learned").

**AI-1.17 / final live-verification round**: after the AOAI credential transfer (see the infrastructure-migration mini case study above), the AOAI AI Gateway dashboard's own key-usage timestamps were used as the authoritative signal over a boolean-only application log line — a genuinely stronger diagnostic than the pattern used in the two prior rounds, since it confirms which credential a request actually authenticated with, not merely whether *a* credential exists. `260b679` reverted the temporary diagnostic once the more conclusive dashboard evidence made it redundant.

---

## Phase 9: Launch-Readiness Auditing, Test-Coverage Growth, and the Finance & Tax Reporting Center (2026-08-18)

**Status: merged to `master`, deployed to production.** The final body of work in this backfill closed out this project's launch-readiness posture and built its financial-reporting layer out from a single expense ledger (Phase 6) into a full ledger/reconciliation/tax-reporting system.

**Testing architecture and regression growth**: a dedicated launch-readiness test-coverage audit (six commits, `9108e05` through `d84bc8d`) found roughly 39 `lib/` files with zero test coverage, including several large, business-critical modules. Rather than blanket-testing every file, each was read in full first and tested only where it contained genuinely pure logic — the established convention throughout this project (`lib/invoice/numbering.ts`'s precedent) of leaving DB-transaction orchestration untested while testing the pure business math those orchestration functions call into. This discipline paid off directly: most of the initially-flagged files turned out to already have their real business logic living in separate, already-tested sibling modules (e.g., `lib/backorders.ts`'s compensation-split math already lived and was tested in `lib/invoice/backorder.ts`), narrowing the real gap to a smaller, genuinely-uncovered set — `lib/cart-store.ts` (sellUnit-aware line matching, 16 new tests), `lib/invoice/calculations.ts` and `validation.ts` (the single-source-of-truth invoice math every PDF, live preview, and server-side validation imports from — 42 new tests combined), `lib/invoice/paymentArrangement.ts` (installment-schedule generation, including its rounding-remainder invariant, 19 tests), `lib/invoices.ts`'s `buildFilterClause` (the one pure function in an otherwise fully Prisma-orchestration file, deciding which invoices appear under each admin filter tab), `lib/fulfillment/alerts.ts` (the bucket-to-alert-type mapping, including the case that a shipment on a since-cancelled invoice must map to `REFUNDED_AFTER_SHIPMENT`, not a generic carrier exception), `lib/invoiceSettings.ts`'s notification-preference default-enabled-unless-explicitly-false rule, `lib/promotions/campaigns.ts`'s discount-validation guard, `lib/customers.ts`'s ZIP-match logic (used in duplicate-customer detection, where a false negative silently misses a real duplicate), `lib/promotions/firstOrderOfferClaim.ts`'s name-splitting and promo-code-generation guarantees, and `lib/portal/profile.ts`'s change-summary logic (which drives the audit-log message for every self-service profile update). Across these fourteen modules, roughly 147 new tests landed in this one sweep alone. Combined with the Finance Center's own 32 new pure-function tests and the AI epic's 148-plus tests (concentrated in the safety matrix and each capability's own unit suite), the project's test suite grew from 626 tests across 59 files (the count recorded at the last case-study update) to **1,419 tests across 131 files** as of `d602c11` (1,401/128 at the Finance Center sprint's own completion; 1,409/129 after the same-day P0 verification pass's formula-injection-guard tests; +10 more across the Finance P1 sprint's `buildQuickBooksXeroExpenseSheet`/`computeMonthBookProfit` unit tests — the Admin cleanup sprint's fixes were pure CSS/layout, with no new logic to test), confirmed by running `npx vitest run` directly against this repository at the time this revision was written — more than doubling in the span this backfill covers, without a single DB-mocked "aggregate report function" test, matching this codebase's consistent preference for testing pure logic directly over mocking Prisma.

**Launch-readiness sprints** (`85958ea`, `c5832a9`, `2d44e37`, `c3c88d5`, `eefeb17`): two real safety/correctness gaps were closed directly — the Order-side shipping-label-purchase route (`app/api/shipping/labels/route.ts`) was missing the `SHIPPO_PURCHASING_ENABLED` kill switch its invoice-side sibling (`lib/fulfillment/labels.ts`) already enforced, meaning it could have bypassed the deliberate "purchasing stays off until Shippo Trust & Safety review clears" gate the moment a live key was installed, regardless of key validity — fixed by reusing the existing, already-tested gate rather than duplicating it; and a Stripe `idempotencyKey` (keyed on `order.id`) was added to checkout session creation, closing a narrow SDK-level-retry risk of creating a duplicate live Session for one order (explicitly scoped as *not* covering a literal double-click, which the existing rate limit plus the per-email concurrent-pending-order cap from Phase 4G already handles). A domain/environment audit found `sitemap.ts`/`robots.ts`/`structuredData.ts` and `layout.tsx`'s `metadataBase` had three inconsistent fallback domains between them — one still pointing at the stale, frozen pre-transfer Vercel URL, one pointing prematurely at `pepscorelab.com` before the domain cutover has actually happened — normalized to the one current, real serving domain and live-verified against the actual rendered `sitemap.xml`/`robots.txt`/canonical/`og:url` tags. A full soft-launch audit (five parallel research passes plus live verification against real production Admin data) produced `docs/launch/PEPSCORE-SOFT-LAUNCH-READINESS.md`, a single GREEN/YELLOW/RED/BLUE dashboard — **zero RED blockers found** for a soft launch of the core application, with the one item that could arguably be RED (real storefront checkout being dark) correctly classified as a deliberate, owner-gated design choice rather than a defect, since the business's actual current sales channel (admin-created invoices) was already fully live and operating. That audit also surfaced two real production-data findings routed to the owner rather than silently actioned: 5 real paid orders awaiting shipping labels (oldest 25 days, blocked on the same Shippo Trust & Safety review as the kill-switch fix above, not an application defect), and — the subject of the next mini case study — leftover rehearsal/test data sitting in production. A same-day follow-up sprint (`eefeb17`) worked the YELLOW list down to a single prioritized `docs/launch/OWNER-LAUNCH-CHECKLIST.md`, producing five new supporting decision documents (`SalesTaxDecision.md`, `CheckoutShippingOptions.md`, `StripeShippoLiveReadiness.md`, `LegalComplianceStatus.md`, `FinalTransactionRehearsal.md`) and correcting two of its own earlier findings that turned out to be overstated on closer inspection — the five legal/policy pages were already fully drafted and live (the "Coming soon" footer fallback is dead code today, not evidence of missing pages), and the Resend DNS gap was narrower than first reported (SPF and bounce-handling MX were already correctly configured; only the `resend._domainkey` CNAME was actually missing).

### Mini Case Study: Financial Reporting Data-Integrity Defect

**PROBLEM**: while archiving 11 leftover "Rehearsal"/test invoices found in production (`c3c88d5` — systematically named `"Rehearsal"`/`"[REHEARSAL] Customer A/B"`, synthetic `@example.com` emails, all created on two consecutive dates, clustered exactly as automated regression-test runs from an earlier autonomous session), archiving them via the app's existing, reversible Archive/Trash mechanism did **not** remove them from revenue reporting — a discovery made explicitly while building the Finance Center's own revenue queries days later, revealing that `archivedAt` and "excluded from revenue" had never actually been the same thing in this codebase's data model, despite that distinction not being visible anywhere until a report was built that depended on it being correct.

**REQUIREMENTS**: every revenue-recognizing query in the system — the Finance Center's own P&L, and the pre-existing Admin Overview dashboard's revenue stat — must be able to exclude non-real transactions permanently and explicitly, independent of an invoice's archive/filing status, which serves a completely different purpose (workflow/visibility) than "was this ever a real sale."

**SOURCE-OF-TRUTH ANALYSIS**: this codebase's deliberate design (see `docs/finance/PEPSCORE-FINANCIAL-ARCHITECTURE.md`, "Sources of truth") treats `Invoice`/`InvoiceItem` as the canonical real-sale record and every `lib/finance/*.ts` report function as a pure reader over existing canonical models — never a second, independently-maintained ledger. Archiving a real, completed invoice is explicitly a *filing* action in this design (it stays in financial history), which is precisely why it was never wired to exclude anything from revenue — the two concepts are legitimately different, but nothing distinguished "test data masquerading as archived" from "a real invoice that happens to be archived" until this fix.

**ROOT CAUSE**: no field anywhere on `Invoice` or `Order` had ever meant "this was never a real transaction." The 11 rehearsal invoices had been archived (a workflow action) but never marked as non-real (a data-provenance fact) — because no mechanism to mark that fact existed at all until this sprint.

**ARCHITECTURAL DECISION / FIX** (`210e3bc`): added `Invoice.isTestData` and `Order.isTestData` — both additive, `Boolean @default(false)`, explicitly documented as distinct from `archivedAt` — and backfilled `true` for the 11 specific rehearsal invoices by their **exact invoice-number list**, never a pattern match against future data (a pattern match against names containing "Rehearsal" or "[REHEARSAL]" would risk silently catching a real future customer named similarly). Both `lib/finance/reports.ts` and the Admin Overview dashboard's own separate, pre-existing revenue query in `lib/invoices.ts` were updated to exclude `isTestData` records — found and fixed in both places specifically because they had never shared one query, meaning a fix in only one would have left the other silently wrong.

**PRODUCTION RESULT**: the previously-reported revenue figure of **$4,978.00** (across 29 invoices) is corrected to **$3,903.00** across 18 genuine invoices (later 15 once archiving/trash state is also accounted for in the Admin view) — a real, quantified $1,075.00 correction to a number the owner would otherwise have used for real business decisions. A same-day follow-up fix (`72a5baf`) then closed a related clarity gap discovered while live-verifying the Finance Center sprint: the Admin Overview's "Revenue (active + archived)" figure and the Finance Center P&L's "recognized revenue" figure use two legitimately different definitions (the former sums all non-cancelled invoice totals including unpaid pending/draft ones; the latter counts only issued/paid invoices) and can show different numbers — $3,903.00 versus $1,184.00 net YTD at the time — that are each correct under their own definition but easy to mistake for a discrepancy. Rather than force the two figures to agree (which would have required redefining one of two legitimately different, independently-useful metrics), a one-line caption was added linking Overview to the Finance Center for the fuller breakdown — a documentation/UX fix, not a numbers fix, since both numbers were already independently correct.

**Finance & Tax Reporting Center — architecture** (`210e3bc`, full detail in `docs/finance/PEPSCORE-FINANCIAL-ARCHITECTURE.md`, `PEPSCORE-TAX-REPORTING-GUIDE.md`, `PEPSCORE-FINANCE-DATA-DICTIONARY.md`): extends the 2026-08-12 Finance & Expense Intelligence module (Phase 6) with Owner Transactions, a Tax Center, Stripe Reconciliation, and Vendor 1099 tracking, following that module's own established tabs-in-one-page IA (eleven tabs total as of this sprint) rather than fragmenting into new routes. Every report function continues to read directly from canonical models — `Invoice`/`Order`/`Payment`/`FinanceExpense`/`InventoryPurchase` — with zero parallel accounting model and zero duplicated money calculation; `profitLoss.ts` and `export.ts` are explicitly pure composition layers over the other report modules, never independent calculators. New schema, all additive: `Invoice.tax` (currently always $0 — nothing collects tax yet, reported honestly as $0 rather than estimated), `OwnerTransaction` (contributions/distributions/reimbursements/owner-paid-expenses — explicitly never counted as revenue or expense), `BusinessTaxProfile` (a singleton, following the same pattern as the existing `InvoiceSettings`), `TaxReminder`, `Vendor1099`, `Form1099KReconciliation`, `MonthlyClose`. Stripe reconciliation classifies every comparison as `MATCHED`/`PARTIAL`/`MISMATCH`/`PENDING`/`NOT_AVAILABLE` (pure, unit-tested); the structured P&L is explicitly labeled "Book Profit," never "taxable income," to avoid any implication of tax advice this system isn't qualified to give; 1099-K reconciliation compares processor-reported gross (entered manually) against book gross computed live; a data-quality-flags module surfaces detectable mismatches for human review and never auto-corrects them; the monthly-close checklist is non-destructive and always reopenable. Vendor/1099 tracking never stores a full TIN or SSN — last-4-digits only, enforced in code, matching `Payment.bankAccountLast4`'s existing precedent from the payment-provider work. The export package grew from 8 to 13 sheets with a deterministic filename. What was explicitly **not** built, named directly rather than silently omitted (`docs/finance/PEPSCORE-FINANCIAL-ARCHITECTURE.md`, "What was explicitly not built"): live bank feeds, automatic bank reconciliation, OCR receipt scanning, AI expense categorization, QuickBooks/Xero integration, automated tax filing, automatic 1099 e-filing, advanced forecasting, automatic estimated-tax calculation, payroll — none block using the Finance Center today, and the export/report boundary is deliberately clean enough that any of these could be added later without rebuilding the underlying model.

**Finance/Tax P0 verification pass** (`a68438f`, `598cc1c`, `6579ba3`, 2026-08-18, same day): a systematic live-browser walkthrough of every Finance Center surface against the original P0 spec — not a re-read of the source, an actual click-through of production with real data — found three genuine, previously-undetected gaps and one live, dollar-quantified defect still active in production revenue reporting at the time.

### Mini Case Study: The Second Test-Data Leak — `isTestData` Backfill Was Incomplete, and Nothing Filtered `deletedAt`

**PROBLEM**: live-verifying the Discounts & Credits report — deliberately chosen because the spec instructed "do not merely verify the Revenue tab" — surfaced two rows billed to "QA Test Customer." The 2026-08-18 `isTestData` fix (see the mini case study above) was believed to have closed this exact defect class three commits earlier in the same day.

**RETRACE**: a direct query confirmed both invoices (`PS-2026-000002`, `PS-2026-000003`) had `isTestData: false` — never covered by the original backfill, which searched only for the later "Rehearsal"-named batch (dated 2026-08-10/11). A broader sweep by customer name pattern (run as a one-off audit query, never as a live filter — see below for why that distinction matters) found four more: `PS-2026-000005` ("Test Customer"), `PS-2026-000010`/`PS-2026-000011` ("TEST DeleteMe" ×2), `PS-2026-000012` ("Jane Test Customer") — all dated 2026-07-21 through 2026-07-24, an entirely separate, earlier round of manual QA that predated the Rehearsal-named batch and used different naming conventions the first backfill's exact-list approach had no way to know about.

**THE MORE SERIOUS FINDING**: `PS-2026-000010` ("TEST DeleteMe," status `ISSUED`, $1.00) was not just untagged — it was soft-deleted (`deletedAt` set) via the admin Trash workflow, yet still being summed into the live Gross Revenue figure. Root cause: `getFinanceDashboardMetrics`'s `invoiceWhere` — and every other invoice-touching query added across `reports.ts`, `salesTax.ts`, `monthlySummary.ts`, `form1099k.ts`, and `dataQualityFlags.ts` during the Finance Center sprint — filtered on `isTestData` and `status`, but had never once filtered on `deletedAt`, even though the older, separate Admin Overview revenue query (`getInvoiceDashboardStats`, Phase 9's first mini case study) already did. Two independently-built revenue queries, one carrying a safeguard the other never inherited.

**FIX**: added `deletedAt: null` to every invoice-touching query across the five files (`6579ba3`) — closing the defect class generally, not just this one row, and specifically *not* dependent on the isTestData backfill being complete, since a soft-deleted invoice should never count in financial totals regardless of its test-data flag. Confirmed by direct before/after query against the exact production where-clause: Gross Revenue (YTD 2026) corrected from **$1,432.00 to $1,431.00**, live-verified in production after redeploy.

**A DELIBERATE NON-FIX, THEN AN OWNER-APPROVED CLOSE**: the six newly-identified invoice numbers were *not* backfilled to `isTestData: true` by pattern-matching customer names containing "test" — the session's own safety controls correctly blocked a direct production financial-data write attempted for this purpose, and the response was to accept that block, report the exact residual (two invoices, $50.00 discount rows each, still appearing in Discounts & Credits), and wait rather than route around it. Pattern-matching against "test"/"QA"/"sample" in a live query was also deliberately not built as a substitute, for the same reason the original backfill used an exact invoice-number list rather than a name pattern: a future real customer could legitimately be named in a way that matches, and a live filter is a permanent, silent behavior change with no review step, where a one-time reviewed backfill is not. The owner reviewed and explicitly approved the exact six-invoice list the same session; the backfill was then applied and reverified by direct query against the same production where-clause the report itself uses — 20 Discounts & Credits rows (down from 22), zero remaining test-customer matches, Gross Revenue unchanged at $1,431.00 (none of the six affected the recognized-revenue set). 17 of 32 total invoices are now correctly flagged `isTestData`.

**PRODUCTION RESULT**: the actual revenue-figure contamination is closed (verified $1.00 correction, live). A second, narrower gap (two non-revenue-affecting discount-report rows) remains open pending one explicit owner-approved data write, tracked as its own item rather than blurred into "done."

**Formula-injection guard on financial exports**: while verifying the CSV/XLSX accountant export flow, found the existing comma/quote escaping in `lib/export.ts` never neutralized a cell value beginning with `=`, `+`, `-`, or `@` — the standard CSV-injection class, where a vendor name, expense description, or note an admin (or, in a future integration, an automated import) enters becomes an executable formula the moment an accountant opens the file in Excel or Sheets. Added the OWASP-documented single-quote-prefix mitigation across every CSV and XLSX sheet-building function in the file, unit-tested both directly (`neutralizeFormulaInjection`) and through a real `buildFinanceExportCSV` call with a malicious vendor-name payload.

**Expense Ledger field gaps closed**: the P0 spec's explicit expense-ledger checklist named three fields the original 2026-08-12 `FinanceExpense` model didn't carry — `recurring`, `taxYear`, and a `reconciliationStatus` distinct from the existing `taxTreatment` (an accounting classification, not a bank-statement-checked marker). Added as additive/defaulted schema fields, wired through the create-expense API, the Add Expense admin form, the Expense Ledger table, and the export sheet — the same path every other field already followed.

**`ORDER_WITHOUT_PAYMENT` data-quality flag**: auditing the data-quality-flags module against the spec's named flag list found most of the "missing" types were correctly absent for a documented, schema-level reason (`PAYMENT_WITHOUT_ORDER` and `DUPLICATE_PROCESSOR_ID` are impossible given existing `@unique`/required-FK constraints; `MISSING_EXPENSE_CATEGORY` is impossible given a required enum column; `STRIPE_TOTAL_MISMATCH` and `REFUND_MISMATCH` are already covered by the dedicated Stripe Reconciliation and Refunds reports rather than duplicated). `ORDER_WITHOUT_PAYMENT` was the one genuinely checkable-but-missing type — added and documented with zero current matches (the `Order` table is empty pre-launch), so the check exists and is exercised by real code before the first live storefront sale, not bolted on reactively after one.

**An infrastructure incident, not a code defect**: the `6579ba3` commit sat on GitHub's `master` branch for over 15 minutes with no corresponding Vercel deployment ever appearing — confirmed genuine (not a dashboard-rendering artifact) via `git ls-remote` against the real GitHub remote and a completely fresh browser tab pointed at the live production Finance page still showing the pre-fix figure. Diagnosed as a one-off Vercel GitHub-webhook delivery gap (three pushes in quick succession may have contributed) rather than anything wrong in the app or the deploy pipeline itself, and resolved via Vercel's own "Create Deployment → Deploy to Production" manual trigger against the `master` branch — the standard, expected recovery path for exactly this situation, not a workaround. Worth a note for future sessions: if a pushed commit doesn't appear in the Deployments list within a few minutes despite `git ls-remote` confirming it's on the remote branch, check for this before assuming a build failure.

## Phase 10: Finance P1 Roadmap + Admin Mobile/Consistency Cleanup Sprint (2026-08-19)

**Status: merged to `master`, deployed to production, live-verified.** With Finance/Tax P0 closed, this phase audited and closed the P1 layer, then swept the entire Admin section for genuine defects — deliberately not a redesign pass against an already-mature, 170+-PR admin IA.

**Finance P1 — implemented** (`600af53`, `fde75da`): a QuickBooks/Xero-ready bank-import CSV export (`buildQuickBooksXeroExpenseSheet`, Date/Description/Payee/Amount/Category/Memo, amounts negated to match both platforms' own "money out" bank-import convention) that needs no paid API connection or credential — the owner downloads and imports it through either platform's existing statement-import feature. And Estimated Tax Planning (Tax Center): a quarterly Book Profit × owner-entered flat rate table, explicitly labeled "Estimate only — not tax advice or a filing," rendering `—` rather than a fabricated `$0` when no rate is set. `computeMonthBookProfit()` mirrors `profitLoss.ts`'s `operatingProfit` calculation exactly rather than re-deriving it a different way, per the project's own standing "never duplicate a money calculation" rule.

**Finance P1 — audited and marked BLOCKED — OWNER/EXTERNAL DEPENDENCY, with exactly what each needs documented** (`docs/finance/PEPSCORE-FINANCIAL-ARCHITECTURE.md`): live bank feeds and real automatic bank reconciliation (a bank-linking provider + real bank credentials), a live QuickBooks/Xero **API** connection distinct from the CSV export just built (a paid subscription + OAuth credentials), automatic tax filing and 1099 e-filing (real government filings, owner/CPA authorization only), and payroll (a licensed payroll provider account and new compliance obligations). OCR receipt scanning, AI expense categorization, and richer receipt-storage metadata were deferred by explicit engineering judgment rather than an external blocker — each is a real, separate scope of work, not a same-pass addition to an already-large sprint.

**Admin cleanup audit**: rather than re-reading the entire admin app from memory, dispatched a targeted read-only audit across every `app/admin/**`/`components/admin/**` surface — AdminNav's own route table (confirmed all 21 nav links resolve to real pages, zero orphaned routes), dashboard, customers, Product Master, invoices, orders, fulfillment, all settings pages, identity-review, intake-queue, promotions, portal-rollout, inventory — checking specifically for dead buttons, broken links, placeholder/lorem copy, dev-only debug UI, and mobile-responsiveness gaps. The result was a strong signal about the codebase's actual maturity: zero `TODO`/`FIXME`/`console.log`/"Coming soon" strings anywhere in the admin tree, zero dead controls, zero orphaned routes — three genuine, narrow defects found instead of the sprawling list a less mature codebase would have produced.

### Mini Case Study: The Notification Bell / Nav Collision

**PROBLEM**: `NotificationBell` was rendered independently in `app/admin/layout.tsx` as `<div className="fixed top-4 right-4 z-50">` — pinned to the literal viewport corner regardless of page content. `AdminNav` separately renders `<div className="ml-auto"><Link>← Storefront</Link></div>` inside its own `sticky top-0 z-[80]` bar. On any viewport at or narrower than the nav's 1600px max-width — i.e. virtually every real screen — both controls resolve to the same physical top-right region, with the nav's higher z-index (`80` vs. the bell's `50`) visually covering the bell wherever they overlapped.

**ROOT CAUSE**: two independently-built global UI elements, each correctly positioned in isolation, that were never checked against each other's actual rendered geometry — a defect class that unit tests and even most manual page-by-page QA passes wouldn't catch, since neither component is "broken" on its own.

**FIX**: rather than nudge coordinates (a change that could just move the collision to a different viewport width), `NotificationBell`'s wrapper was changed from an independently `fixed` element to a normal `relative` one, and it was moved to render *inside* `AdminNav`'s own flex row, next to the Storefront link. This doesn't just fix the current collision — it makes a future one structurally impossible, since both controls now share one layout container instead of two independently-positioned ones.

**PRODUCTION RESULT**: live-verified via screenshot on the deployed site — the bell now renders cleanly inline with "← Storefront," no overlap at any width the nav itself already handles (including its own `overflow-x-auto` mobile behavior, which the bell now correctly participates in rather than staying anomalously fixed while everything else scrolls).

**Other fixes in the same sprint**: a payment-analytics table (Settings → Payments) and 9 tables across the two first-party Intelligence pages and the AI Control Panel were missing the `overflow-x-auto` wrapper every other admin table in the codebase already has — the same defect class, found by grep once the first instance was identified, not by re-auditing each page individually. An inventory pricing-confirmation modal was missing the `max-h-[90vh] overflow-y-auto` cap the codebase's own established `Modal` pattern (`SellUnitCorrectionDialog`) already uses elsewhere, risking the Confirm/Cancel buttons being pushed off-screen on a short viewport.

**A live, incidental confirmation of the earlier Finance fix**: while screenshotting the fixed nav, the Admin Overview's "Revenue (active + archived)" figure was observed to have dropped from $3,903.00 to $3,653.00 between sessions — not a new bug, but the expected, automatic effect of the owner-approved 6-invoice `isTestData` backfill (Phase 9) propagating through `getInvoiceDashboardStats()`'s pre-existing `isTestData: false` filter, exactly as designed. $3,903 − $3,653 = $250, matching the sum of the three backfilled invoices' totals that weren't already excluded by that query's separate `deletedAt: null` filter (`PS-2026-000002`/`000003`/`000005`, $75 + $75 + $100) — confirmed by direct arithmetic, not assumed.

---

## 16. Portfolio Summary

Pepscore Lab is a production back-office platform — invoicing, payment arrangements, carrier-agnostic shipment tracking, a real fulfillment engine, CRM, pricing intelligence, reorder/repeat-purchase workflows, a centralized notification design system, storefront checkout with server-side promotion redemption, and a Customer Portal — built for a peptide-research-supplier business through owner-directed, AI-assisted engineering with Claude Code. The owner defined product vision, requirements, architecture direction, business rules, and QA/production-validation standards; implementation, testing, and bug discovery were carried out under that direction and recorded in a 65-entry engineering decision log spanning 170+ merged pull requests. The system reflects disciplined engineering practice throughout: provider abstractions reused across shipping and payments, derived-not-stored state to prevent drift, layered kill switches and fail-closed defaults on every real-money and real-communication path, shared concurrency-safety primitives (optimistic inventory locking, `FOR UPDATE` row locks) applied consistently rather than per-call-site, and an audit-before-building discipline that caught and fixed several genuine production bugs (a silently-dropped Stripe refund webhook, an unreserved storefront inventory path, a cron misconfiguration silently blocking every deploy, a 10x-under-reservation bug in the sell-unit-aware cart, a concurrent-reservation race on the last unit of stock, a portal-invite duplicate-send race) before they became customer-facing incidents. The operational core — invoicing, fulfillment, tracking, CRM, pricing, reorder, notifications, and now checkout/promotions — is production-validated and in active use; real payment activation, real bulk customer communication, and real postage remain deliberately held behind activation switches pending explicit business go-ahead, exactly as documented in the project's own payment-readiness report and `docs/PendingOwnerActions.md`.

---

## Continuous Update Rule

This document is not a final-week deliverable — it is maintained alongside development, updated as part of normal execution rather than as a separate documentation checkpoint. The standing rule: after a meaningful PR finishes implementation, testing, and merge/deploy/verification, and after the roadmap is reconciled, this case study and its Evidence Index are updated (or explicitly evaluated and judged not to need an update, for purely mechanical/formatting-only changes) before moving to the next task. Historical backfill (sections 1–16 above) was performed once, grounded in `docs/Decisions.md`, `docs/ChangeLog.md`, `docs/ComponentMap.md`, `docs/ProductRoadmap.md`, and `git log`, current through PR #135; subsequently extended incrementally through PR #144 (Decisions #43–#47, the 5-slice email-shell migration), through PR #163 (Decisions #48–#60, closing Phase 3B/3C/3D in full), through PR #195 (Phases 5/6, the homepage-refresh/compliance sprint and the Admin Portal Master Optimization + Finance & Expense Intelligence sprint), and — as of this revision — through commit **`d602c11`** (Phases 7 through 10: the RBAC migration, catalog-taxonomy and family-image/asset standardization, the full Pepscore Intelligence AI epic from AI-0B through AI-1.18, the launch-readiness test-coverage sweep, the soft-launch audit sprints, the Finance & Tax Reporting Center and its P0 verification pass, the Finance P1 roadmap audit and its two implemented capabilities, and the Admin mobile/consistency cleanup sprint). That gap — 55 commits spanning 2026-08-14 through 2026-08-18 — was found and backfilled in one pass on 2026-08-18 specifically because `docs/Decisions.md` and `docs/ChangeLog.md` were not updated in step with that work (both stop at PR #193/#194, 2026-08-11); this document itself, not those logs, is the current source of truth for anything after that date until a future revision closes the same gap in those logs, if one ever does. Every subsequent meaningful commit is expected to extend this document rather than requiring a second backfill pass — if a future gap is found between the last documented milestone and current `git log`, the correct response is to backfill only that gap, not to regenerate the whole document. Status labels (Completed / Production Validated / Engineering Complete — Activation Pending / Deferred / In Progress / Planned) are re-checked against `docs/PaymentReadiness.md`, `docs/launch/PEPSCORE-SOFT-LAUNCH-READINESS.md`, and current production configuration whenever a labeled system changes, not assumed to stay accurate indefinitely. A companion portfolio-safe layer (`docs/PortfolioCaseStudy.md`) and a Mermaid architecture-diagram reference (`docs/ArchitectureDiagrams.md`) are maintained alongside this document as of this revision — both should be reconciled against this document whenever it changes materially, per the standing case-study-is-part-of-definition-of-done rule this project now follows for every meaningful production sprint.
