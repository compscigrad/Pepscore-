# Pending Owner Actions — Consolidated

**Purpose**: one canonical list of every item in this project that genuinely requires the owner's own action (a credential, a dashboard click, a business decision only the owner can make, a real-money/real-communication go-ahead) — so autonomous engineering work never has to rediscover or re-ask the same question twice. Engineering work never stops on any single item here; independent work continues while an item stays pending. When an item is resolved, it's marked resolved in place rather than deleted, so the history stays legible.

This document indexes items already tracked in more detail elsewhere (`docs/PaymentReadiness.md`, `docs/Decisions.md`) rather than duplicating their full write-ups — follow the link for the complete context on each.

---

## Launch-readiness snapshot (2026-09-03 post-sprint)

Classified per the owner's 5-bucket scheme. Superseded items from the 2026-08-31 pass have been folded in or resolved below rather than kept as a second stale list.

**COMPLETED AUTONOMOUSLY THIS SPRINT (nothing to do — informational):**
- Direct Sales: custom-discount label is no longer required to save (blank now stores "Miscellaneous"); line-item and invoice-level discounts now show original price / discount / final price explicitly in the admin editor, the live preview, and both PDFs; saved promotions can be deleted/deactivated directly from the Discounts panel without corrupting historical invoices; discount inputs now reject a >100% percentage and a line discount larger than the line itself.
- Direct Sales: fixed a real bug where a fresh $0-total draft (e.g. Quick Intake) displayed "Paid in full." — now correctly shows a neutral "no balance due yet" state; genuine full payment still shows "Paid in full."
- Customer lifecycle: true-delete (dependency-checked — invoices, orders, credits, saved payment methods, Price Match/Professional/redeemed-promotion history all block it) and Close/Archive (wired the existing-but-unused `archiveClosedCustomer`, added an admin-initiated `adminCloseCustomerAccount`) are both live on the customer profile page.
- Portal eligibility: found and fixed the real root cause of "manually entered Direct Sales customers show Lead/Not Eligible" — the Quick Intake route built its invoice via a raw Prisma call that skipped the customer-status recompute every other invoice-creation path runs. Fixed going forward, and backfilled the 4 real customers already caught in that state (confirmed against the live database, not a blind mutation).
- Birthday promotion — completed end-to-end this pass (2026-09-04 birthday-journey sprint): customer-portal birthday month/day entry/edit form (`/account/profile`, separate from Clerk identity, both-or-neither validated, Feb 29 accepted); checkout/cart promo-code entry now resolves and applies a birthday code the same way as any other promo, with the discount broken out visibly ("Price Match Subtotal"/"Eligible Merchandise" → "Birthday 15%" → "Adjusted Merchandise Total"); understandable INVALID/EXPIRED/ALREADY-USED/PROFESSIONAL-INELIGIBLE messaging; one-time redemption is enforced with the same idempotent soft-hold/finalize-on-payment discipline the existing `PromotionCode` system uses (`Order.appliedBirthdayCodeId`, `finalizeBirthdayRedemption()` called from `markOrderPaid()`); the daily cron (`/api/cron/birthday-promotions`) is now registered in `vercel.json` (13:00 UTC) — the cron itself safely no-ops until the owner sets `BIRTHDAY_PROMOTIONS_ENABLED=true` (see CAN DO NOW below, narrowed from "schedule the cron" to just the launch flag).
- 2D label + 3D storefront nomenclature correction (verified again this sprint: dimensions/DPI/PDF page size all still exactly correct, "+" → "/" separator, NAD+ unchanged) — carried over from 2026-08-31, re-verified programmatically.

**CAN DO NOW — no entity/EIN, no engineering blocker:**
- Set `BIRTHDAY_PROMOTIONS_ENABLED=true` in Vercel once you're ready for real birthday discount codes to start reaching customers — the cron is already scheduled in `vercel.json` (daily, 13:00 UTC) and no-ops harmlessly until this flag is set; same "built and safety-gated, owner flips the one real-send switch" pattern as the first-order-offer reminders. This is the only remaining birthday-feature action — everything else (issuance logic, checkout redemption, portal profile UI, admin visibility) is complete and tested.
- Twilio: purchase/port a phone number, set `TWILIO_ACCOUNT_SID` / `TWILIO_AUTH_TOKEN` / `TWILIO_PHONE_NUMBER` in Vercel, point the number's (or Messaging Service's) "A message comes in" webhook at `/api/webhooks/twilio`, and send one real test STOP/START from an owner-controlled phone (item #3, steps 2–4). This environment has no browser-automation tool available, so none of this could be done by Claude directly this sprint — see the Twilio/Google Voice section of the final report for the exact reason.
- Google Voice setup as the human-facing business line — also requires a browser session; not attemptable from this environment. Sign in at voice.google.com, claim/port a number, set voicemail/greeting.
- PayPal Dashboard enablement (item #2), legal/policy page review (item #9), RUO wording review (item #12), Clerk phone-MFA setup (item #11).

**WAITING ON PEPSCORE LLC/EIN:**
- Twilio A2P 10DLC Brand + Campaign registration itself (item #3, step 1) — requires the final legal business name/EIN/address. Explicitly must NOT be submitted under a temporary or incorrect identity to save time; wait for the real entity.
- Any other provider verification that requires a legal business identity document (EIN letter, Articles of Organization, etc.) as part of its own review — Shippo's Trust & Safety business-registration review (item #4) falls here if/when their review asks for Articles of Organization specifically; current production shipping (Pirate Ship + manual tracking) is unaffected either way, so this is not urgent.
- Stripe live activation (item #1) ultimately needs a real business entity behind the merchant account too, though the immediate blocker there is the compliance review, not a specific document.

**WAITING ON EXTERNAL PROVIDER / TOOLING:**
- Google Voice and Twilio console setup both require a real browser session — no browser-automation tool is connected to this environment. This is a capability gap, not a business-entity blocker; if a browser tool becomes available (or you'd rather grant specific API credentials instead), this can move.

**POST-LAUNCH / OPTIONAL:**
- (none carried over from the birthday feature — see COMPLETED AUTONOMOUSLY above)

---

## Open items

| # | Item | Area | Exact owner action required | Why it's owner-only | What's already done | Blocks launch or one feature? | Status |
|---|---|---|---|---|---|---|---|
| 1 | Stripe live activation | Payments | Request live (non-test) Stripe API keys, set them in Vercel, flip `STOREFRONT_CHECKOUT_ENABLED` on. Confirm RUO merchant-eligibility/compliance review before doing so. | Real API keys and a compliance judgment call are owner-only; code path is fully built and test-mode verified (`docs/PaymentReadiness.md` §§2–11). | Card, ACH, Cash App Pay, PayPal all engineered and test-mode verified end-to-end. | Blocks launch of real storefront checkout only — everything else in the app is unaffected. | Open (as of 2026-08-08 readiness report) |
| 2 | PayPal Dashboard enablement | Payments | Enable PayPal as a payment method for the Stripe account in the Stripe Dashboard (Stripe itself enforces this, not app code). | Dashboard-side account configuration, owner-only. | App-side `paypalEnabled` gate built and wired (Decision #32). | Blocks PayPal specifically, not checkout overall (card/ACH unaffected). | Open |
| 3 | Twilio A2P 10DLC registration | SMS | 5-step checklist: register Brand/Campaign, purchase/port a number, set `TWILIO_*` env vars in Vercel, point the number's inbound webhook at `/api/webhooks/twilio`, send one real test STOP/START. Full detail: `docs/PaymentReadiness.md` §13. | Carrier/provider registration requires the owner's own business identity and Twilio account. | Sending + receiving (STOP/START webhook, signature-verified) both fully engineered (Decision #27). | Blocks all real SMS (transactional and any future bulk) — email notifications are unaffected. | Open |
| 4 | Shippo Trust & Safety business-registration review | Shipping | Complete Shippo's account review process for the business. | Third-party account verification, owner-only. | Tracking (real, verified against Shippo's test API) and manual-tracking fallback both production-live already; only label **purchasing** (real postage spend) is gated. | Blocks real label purchasing only — manual tracking (Pirate Ship + manual entry) is the actual current production shipping workflow and is unaffected. | Open, deliberately deferred (Decision-log precedent: not reopened as a blocker) |
| 5 | Resend domain verification for `pepscorelab.com` | Email | **Narrowed 2026-08-18 by direct DNS lookup**: `send.pepscorelab.com`'s SPF (`v=spf1 include:amazonses.com ~all`) and bounce-handling MX (`feedback-smtp.us-east-1.amazonses.com`) are both correctly in place today. Only `resend._domainkey.pepscorelab.com` (CNAME) is missing — check the Resend Dashboard for the exact required value and add just that one record. | DNS record change on the owner's own domain. | Every email template (all 18, Decisions #43–#51) is fully built and shell-branded; Reply-To already correctly shows `admin@pepscorelab.com`/etc. regardless of this — only the raw `From:` header and DKIM signing depend on it. | Not purely cosmetic — without DKIM, mail authentication is weaker and more likely to land in spam even once Resend shows the domain as otherwise verified. Does not block any functionality. | Open, narrowed 2026-08-18 |
| 6 | PortalRolloutSettings activation | Customer Portal | The one explicit admin action that triggers real bulk portal-invite sends to the live customer base, after reviewing the launch-readiness report (eligible audience, review-queue count, template/provider readiness). | Real bulk customer communication — an explicit standing stop condition, never flipped autonomously. | Rollout automation, eligibility computation, dry-run/allowlist/per-run-cap safety gates, and delivery-failure surfacing are all built and live (Decisions #35–#40). | Blocks real bulk portal invitations only — admin can still invite individual customers one at a time today. | Open |
| 7 | Customer Portal live customer-login QA walkthrough | Customer Portal | One authenticated walkthrough of `/account/orders` and `/account/tracking` from a real (non-admin) customer login, to close out the QA checkpoint recorded in project memory. | Requires a real customer-side Clerk session distinct from the admin session this environment has authenticated access to. | `/account/orders` + `/account/tracking` shipped and tested from the admin/engineering side. | Optional QA closure — does not block any shipped functionality, just final sign-off. | Open (tracked in memory as `project_pepscore_portal_orders_qa_checkpoint`) |
| 8 | ~~Storefront Order visibility in Customer Portal~~ | Customer Portal | ~~A real design/engineering decision...~~ | — | — | — | **Resolved 2026-08-10** — re-verified by direct code trace (Phase 4B journey audit) and found stale: `/account/orders` (`lib/portal/orders.ts`) already correctly queries real storefront `Order`/`Payment` data, `userId`-scoped, not admin-invoice records. The real, narrower gap actually found — `Invoice.customerId` was never set at checkout, so `/account/invoices`/`/account/tracking` (both `customerId`-scoped) never showed a storefront order's linked invoice or shipment — was fixed directly in PR #175, not left as an owner action. |
| 9 | Legal/policy page content (Terms of Service, Privacy Policy, Shipping Policy, Returns & Refunds) + Lab Results/COA program | Legal / Foundation | **Corrected 2026-08-18** — this entry was stale; all five pages were fully drafted and wired live in an earlier pass (2026-08-12) and this entry wasn't updated to reflect it. Remaining action is much narrower: review `/terms /privacy /shipping /returns /lab-results` (all live, all self-marked "OWNER/LEGAL REVIEW REQUIRED" in code comments), fill in the one explicit blank (Governing Law jurisdiction in Terms), confirm the "third-party tested"/COA claim on the Lab Results page matches real supplier documentation, and give final sign-off (directly or via counsel). | Legal-language sufficiency and the governing-law jurisdiction require the owner's own business/legal judgment — code must never fabricate or materially rewrite this content (standing Phase 4R rule), and this environment does not know Pepscore Lab's registered entity/state of formation. | All five pages exist, are live (confirmed 200 OK), are linked from the footer (the old "Coming soon" tooltip fallback is dead code today — every link has a real `href`), and are deliberately excluded from search indexing (`robots: { index: false }`) until sign-off. Full detail: `docs/launch/LegalComplianceStatus.md`. | Blocks nothing functionally (pages are live today), but real sign-off is recommended before real checkout activates and before indexing is turned on. | Open, narrowed 2026-08-18 |
| 10 | Confirm Neon database backup/point-in-time-recovery retention window | Infrastructure | Check the Neon dashboard directly for the account's actual plan tier and its specific PITR retention window (Neon's paid tiers provide automatic PITR by platform default, but the exact retention length is plan-dependent). | This environment has no visibility into the Neon account's billing/plan tier or dashboard — requires the owner's own account access. | Nothing code-side to build here — this is a platform capability, not an application feature; `.env.local.example` already documents every required env var for a from-scratch environment rebuild. | Doesn't block launch — a real backup mechanism already exists by default on Neon's paid tiers; this only confirms exactly how far back it reaches. | Open |
| 11 | Enable phone number as a Clerk sign-in factor + configure step-up/MFA policy for new/untrusted devices | Auth / Security | In the Clerk Dashboard: **User & Authentication → Email, Phone, Username** → enable Phone number as a verification factor. Then **User & Authentication → Multi-factor** → enable SMS as an MFA option (uses Clerk's own SMS delivery, a separate system from the app's Twilio account — see item 3, they are not coupled). For device-based step-up (new/untrusted device requires a second factor, trusted device stays smooth), review **Sessions** → device/session settings and **Attack Protection** in the Dashboard for the current plan's device-trust behavior — exact controls are plan-tier-dependent and this environment has no Dashboard visibility to confirm which are already active. | Requires the owner's own Clerk Dashboard access; MFA/device-trust policy is an account-level security decision, not an app-code change. | Confirmed by direct inspection of the live (test-mode) sign-in UI: only Google OAuth and email/password are currently offered — no phone/SMS option. `components/account/AccountSecuritySection.tsx` already correctly defers all MFA/passkey UI to Clerk's own native `<UserProfile>` component (no homemade OTP system to duplicate). | Doesn't block launch — email/password sign-in already works correctly and is protected by Clerk's own standard session security. Adds a stronger, owner-requested step-up experience for new/untrusted devices once enabled. | Open (added 2026-08-12) |
| 12 | Legal review of the RUO acknowledgment wording | Legal / Compliance | Updated 2026-08-12 to version `RUO-2026-01` with owner-provided copy (age confirmation + RUO/Terms/Privacy agreement, two distinct checkboxes). Review `lib/compliance/ruo.ts` for legal sufficiency. If wording changes again, bump `RUO_VERSION` in the same file — every signed-in customer will then be prompted to re-accept once, automatically; guests already re-accept every checkout regardless of version. | Legal-language sufficiency requires the owner's/legal counsel's own judgment. | Full versioned acceptance workflow built and tested: `ComplianceAcknowledgment.version`, `hasAcceptedCurrentRuo()`, `/api/compliance/ruo/status` + `/accept` routes, checkout skips the modal for an already-current-version-accepted signed-in customer, and real `/terms`/`/privacy` links are embedded in the agreement checkbox text. | Doesn't block anything today. | Open (updated 2026-08-12) |
| 16 | ~~Bulk case-quantity discount tiers are marketing copy only, not enforced at checkout~~ | Pricing / Storefront | ~~The homepage's "3–4 cases · Save 5%" etc. tiers are informational...~~ | — | — | — | **Resolved 2026-08-19** — the 5/8/10/15% ladder is now the real, canonical, server-side-enforced pricing engine (`lib/pricing/canonicalPricing.ts`), applied automatically at checkout (aggregated across every qualifying case in the cart, any product) and in admin-created invoices. Never a coupon code. Explicitly does not stack with Professional Access pricing, per the same sprint's locked decision. |
| 27 | ~~Tesamorelin Professional pricing conflicts with the intended hierarchy~~ | Pricing | ~~Tesamorelin 10mg: Standard $775, current Professional price $700...~~ | — | — | — | **Resolved 2026-08-19 (Closure Pass)** — owner-approved: Tesamorelin 5mg Professional $355 → $320 (Standard $395 unchanged, 15+ tier $335.75); Tesamorelin 10mg Professional $700 → $625 (Standard $775 unchanged, 15+ tier $658.75). Both now more favorable than the deepest standard-volume tier. Applied via `setActivePricing()` (the real, audited write path — `ProductPriceChange` rows recorded), not a raw update. Verified in production DB and covered by 5 new regression tests pinning these exact figures (`lib/pricing/canonicalPricing.test.ts`). |
| 18 | Individual Vial pricing formula not yet redefined | Pricing | The 2026-08-12 revision retired the old Standard/SPA formula (now supplier cost ×4 / Retatrutide-derived ~0.705 ratio) but gave no replacement for Individual Vial — it's deliberately left on the old per-vial multiplier (`INDIVIDUAL_VIAL_MULTIPLIER` in `lib/pricing/engine.ts`) rather than guessed. Confirm whether Individual Vial should also move to a new formula, and if so, what it is. | A commercial pricing decision only the owner can make. | Standard/SPA fully recalculated and applied across the whole non-protected catalog; Individual Vial intentionally untouched pending this answer. | Doesn't block the preview — Individual Vial is only publicly visible for the 8 owner-approved products, none of which had their case price change enough to make the current vial price obviously wrong. | Open (added 2026-08-12) |
| 19 | Master pricing report — prepared, not sent | Pricing / Correspondence | `docs/MasterPricingList-2026-08-12.csv`/`.md` generated; `lib/pricing/sendMasterPricingReport.ts` is a ready-to-call function (not wired to any UI button or automation) that emails the finalized list to `admin@pepscorelab.com` with the CSV attached, logged through the standard `Communication` correspondence record. Call it (or ask for it to be run) once you've reviewed and approved this pass's pricing. | Real business-communication send — never triggered automatically, matching the standing "no bulk/business communication without explicit approval" rule this whole session. | Email template, routing category (`ADMIN_PRICING_REPORT`), and CSV attachment all built and typechecked. | Doesn't block the preview. | Open (added 2026-08-12) |
| 21 | Finance: no real file-upload for expense receipts | Finance / Infrastructure | `FinanceExpense.receiptUrl`/`receiptFilename` are plain text fields — the admin pastes/types a URL (e.g. a link to a receipt already stored somewhere), there is no drag-and-drop upload anywhere in this codebase today (confirmed by a repo-wide search — no Vercel Blob/S3/Supabase-storage integration exists). Real upload would need a new storage integration provisioned first. | A new third-party integration decision (which provider, cost) is the owner's call, not something to silently add. | Expense entry, filtering, reporting, and export all work fully without it — receipts are just referenced by URL for now. | Doesn't block Finance from being useful today; only blocks true in-app file upload. | Open (added 2026-08-12) |
| 28 | Price Match: no real screenshot/image upload for proof | Price Match / Infrastructure | `PriceMatchRequest.proofUrl`/`proofNote` are plain text fields — the customer pastes a link to a screenshot/listing rather than uploading a file directly, same precedent and same root cause as item 21 (no file-storage integration exists anywhere in this codebase). Real upload would need the same new storage integration item 21 already needs — one provider decision would resolve both. | Same third-party integration/cost decision as item 21, owner-only. | Submission, admin review, and approval all work fully with a pasted link — most competitor evidence (a live product page URL) doesn't need a screenshot anyway. | Doesn't block the Price Match workflow; only blocks true in-app image upload for proof. | Open (added 2026-08-20) |
| 29 | Scheduled Research Reorders — future capability, read-only readiness assessment only | Pricing / Commerce | Not implemented and not activated. A future "Scheduled Reorder" (recurring/scheduled procurement, framed as convenience/automation rather than a discount) would need, before activation: (1) a payment-processor recurring-billing decision — Stripe supports it technically, but nothing should activate merely because of that; (2) catalog eligibility rules (which products/customers qualify); (3) RUO-safe customer-facing language (never consumer "subscription" framing); (4) consent/authorization requirements for the recurring charge; (5) a customer-facing management surface (pause/skip/cancel/change cadence/update payment method — cancellation must not require Admin approval, mirroring the account-closure precedent); (6) confirmation that every scheduled occurrence re-resolves canonical pricing fresh (Standard/Volume/Professional/Preferred Pricing) rather than ever copying a stale historical price, the same rule repeat orders already follow. | Activating real recurring charges is a business/processor decision, not an engineering one — inventing the terms risks a customer-facing commitment the business never agreed to. | The existing repeat-order architecture (`lib/storefront/reorder.ts`) already proves the "always re-resolve current pricing, never copy historical" pattern a scheduled version would reuse. Documented as a Policy Center entry (`scheduled-reorder-not-active`, status DRAFT) so the current "not active" state is explicit, not silently assumed. | Blocks nothing — no code path references or requires this feature today. | Open (added 2026-08-20, Customer Portal Maturity sprint appendix) |
| 22 | ~~Finance: Invoice-line COGS has no admin input UI yet~~ | Finance | ~~`InvoiceItem.costOfGoods`...~~ | — | — | — | **Resolved 2026-08-19** — a "COGS (per unit)" input was added directly to the invoice builder's line-item editor (the option this entry itself named as "the obvious place"), defaulting from the matched product's `Product.costOfGoods` when available (correctly scaled for a case/SPA/bulk sell unit), always admin-editable. New invoices now populate real COGS going forward. Historical backfill for pre-existing invoice lines remains a separate, optional owner decision — see item 23. |
| 23 | Finance: no historical backfill performed | Finance | This sprint intentionally did not backfill any pre-existing shipping cost, discount, or expense history into the new `FinanceExpense`/`InventoryPurchase` ledger — per the spec's own "run a dry-run report first, classify safely-backfillable vs. ambiguous vs. owner-review-required, never invent old expenses" instruction. The ledger starts empty and grows from real entries (manual + automatic) going forward. | Whether historical backfill is even wanted, and how to classify ambiguous old records, is a business decision. | The Finance dashboard/reports/export all correctly show real numbers only for whatever range has real entries — no fabricated history. | Doesn't block using Finance going forward. | Open (added 2026-08-12) |
| 24 | ~~Price-Matching Guarantee — mechanics not yet defined~~ | Public Copy / Policy | ~~The Mission section named a "price-matching guarantee" with no operational policy behind it.~~ | — | — | — | **Resolved 2026-08-20 (Price Match Guarantee sprint, Decisions #77/#79)** — a real, database-backed system now exists: public `/price-match` request form (delivered-price comparison basis, proof-as-URL), admin `/admin/price-match` review queue (approve/reject/request-more-info), `PriceMatchAuthorization` grants an exact admin-set price under one of three durations (one purchase / until a date / until revoked), enforced server-side by the canonical pricing engine (never a coupon code, never a customer-typed discount). Eligibility/threshold/duration are still admin discretion per request (no auto-approval rule), which is the intended design — every request is reviewed by a human, not auto-matched against a fixed percentage. |
| 25 | RUO/21+ signup gate — recommend one live manual click-through | Auth / Compliance | The pre-signup gate (`app/sign-up/[[...sign-up]]`) was verified thoroughly server-side (unauthenticated gate render, invalid-input rejection, valid-acceptance cookie issuance, gate lifting, forged-cookie rejection, direct-URL variants — all against production directly) and its account-linking logic was verified against the live database via a rehearsal script. What was **not** possible this session: an actual mouse-driven click-through of Client Sign In → Sign up → gate → real Clerk account creation, and a real mobile-viewport visual check — the Chrome browser extension was not connected. | Requires either a real browser session for Claude, or the owner doing one manual pass. | Every server-enforced behavior (the actual security boundary — no bypass exists) was verified directly; only the visual/interactive polish is unconfirmed. | Doesn't block production — the gate is live and enforced either way. Recommended before wide announcement of account creation. | Open (added 2026-08-12) |
| 26 | Confirm Clerk production keys before wide traffic | Auth / Infrastructure | Every Clerk-authenticated route (`/account`, `/admin/**`) currently redirects through a `clerk.accounts.dev` cross-domain "dev browser" handshake before rendering — the standard behavior for Clerk's development-instance API keys, confirmed while diagnosing what first looked like a production outage (it wasn't; a real browser completes this automatically and invisibly). Real browsers handle it fine, but it depends on third-party cookies to `*.clerk.accounts.dev` not being blocked, which some strict browser privacy modes or embedded/in-app webviews do block. Confirm in the Clerk Dashboard whether this app is still on development keys, and if so, whether/when to switch to production keys (a real account-level change, not an app-code one). | Requires the owner's own Clerk Dashboard access to confirm current key mode and make the switch. | Nothing code-side to fix — this is a Clerk account configuration question, not a bug. | Doesn't block most real users (modern browsers handle the handshake transparently) — worth confirming before any push into contexts with stricter third-party-cookie policies. | Open (added 2026-08-13) |

---

## Resolved items

| # | Item | Resolution date | How resolved |
|---|---|---|---|
| 16 | Bulk case-quantity discount tiers not enforced at checkout | 2026-08-19 | Professional Access sprint: built `lib/pricing/canonicalPricing.ts`, the canonical server-side pricing engine, and wired the locked 5/8/10/15% ladder into checkout and admin invoices. Real, automatic, aggregated across every qualifying Standard Case line in an order — never a coupon code, never stacking with Professional pricing. |
| — | (P0) Checkout trusted client-submitted `sellUnit` with no Professional-entitlement check | 2026-08-19 | Found by the preceding read-only pricing audit, fixed same sprint: `resolveCanonicalPricing()` now requires a server-resolved `proEligible` (Clerk userId only, never email match) before returning Professional pricing; an unauthorized `CASE_PRO` request throws before any price is computed. 39 new tests cover this directly. |
| 27 | Tesamorelin Professional pricing conflicts with the intended hierarchy | 2026-08-19 | Owner-approved: 5mg Professional $355 → $320, 10mg Professional $700 → $625 (both Standard prices unchanged). Applied via the audited `setActivePricing()` path, verified in production, 5 new regression tests. |
| 24 | Price-Matching Guarantee — mechanics not yet defined | 2026-08-20 | Built end-to-end: `PriceMatchRequest`/`PriceMatchAuthorization` models, public `/price-match` request form (delivered-price basis), admin `/admin/price-match` review queue, exact-price/one-of-three-durations authorization enforced by the canonical pricing engine as a new non-stacking `PRICE_MATCH` precedence layer. Rehearsed end-to-end against real Postgres (submit → approve → resolve → redeem → revoke), all 6 scenarios passed. |
| 22 | Finance: Invoice-line COGS has no admin input UI yet | 2026-08-19 | Added a "COGS (per unit)" input to the invoice builder's line-item editor, defaulting from `Product.costOfGoods` when available (scaled correctly for case/SPA/bulk sell units, matching the exact per-line-quantity-unit convention `lib/finance/reports.ts` already read), always admin-editable. Historical backfill remains optional/owner-discretionary per item 23's same standing rule. |
| 8 | Storefront Order visibility in Customer Portal | 2026-08-10 | Re-verified by direct code trace (Phase 4B) and found stale — `/account/orders` already correctly shows real storefront `Order` data. The real gap found (`Invoice.customerId` unset at checkout, breaking `/account/invoices`/`/account/tracking`) was fixed in PR #175. |
| 13 | Reactivate CJC-Ipamorelin-without-DAC / Botulinum Toxin / KLOW | 2026-08-12 | Owner confirmed and directed reactivation. All three set `pricingStatus = ACTIVE` with formula-derived pricing from the 2026-08-12 wholesale source; Botulinum Toxin's unit corrected to "100 units" (was "100mg"); Standard Case enabled on all three, public Individual Vial enabled only for CJC-Ipamorelin-without-DAC per owner instruction (Botulinum/KLOW keep it stored but hidden). KLOW's Standard Case later fixed to the owner's explicit $960 override (see item 17 below). **Superseded same day for Botulinum Toxin only — see item 20.** |
| 20 | Botulinum Toxin re-archived and renamed | 2026-08-12 | The pharmaceutical lab notified the owner that availability is uncertain; product renamed `Botulinum Toxin` → `Botulinum Toxin Type A` (unit remains "100 units") and `pricingStatus` set back to `INACTIVE`. Two real gaps found and fixed while doing this: (1) `/api/checkout` never checked `pricingStatus` at all, so a stale cart or a direct API call could still fully check out an archived product regardless of browse-side filtering — now blocked explicitly. (2) The admin invoice product picker (`app/admin/invoices/new`, `app/admin/invoices/[id]`) filtered only on the legacy `inStock` flag, letting an admin hand-select an archived product onto a new invoice — now also excludes `pricingStatus: INACTIVE`. Historical invoices/orders/pricing/inventory records are untouched; a new `AdminAuditLog` entry records the rename+archive. |
| 15 | SPA pricing inconsistency for AOD-9604, TB-500, HCG | 2026-08-12 | Fixed directly — SPA recalculated for all three using the Retatrutide-derived ratio (~0.705 × the new Standard Case), verified `SPA < Standard` for every row. |
| 17 | Retired old pricing formula, applied new ×4/Retatrutide-ratio model catalog-wide | 2026-08-12 | `lib/pricing/engine.ts` updated (`STANDARD_CASE_MULTIPLIER = 4`, `SPA_TO_STANDARD_RATIO = 0.705`, commercial rounding to nearest $10, SPA<Standard invariant enforced in the formula itself). Applied to all 63 non-protected active products via a live recalculation script; KLOW given its explicit $960 owner override separately. Added a hard `SpaPricingInvariantError` guard in `lib/pricing/service.ts`'s `setActivePricing()` so a future admin edit can never leave SPA ≥ Standard again — the error carries a proposed corrected SPA using the same ratio. |
| 14 | Real "buy an Individual Vial instead of a case" purchase flow | 2026-08-12 | Built: `ProductCard.tsx` now renders a real Standard Case / Individual Vial toggle for every product with a publicly-enabled individual price, and `handleAdd()` passes the selected `sellUnit` through to the cart. Verified this correctly propagates through the pre-existing sell-unit-aware cart/checkout/reservation architecture (`resolveCheckoutLine()`, `computeVialsToReserve()`) via a live rehearsal script covering all 8 public-vial products plus all 4 hidden-vial products (Botulinum/KLOW/Tesamorelin/AOD-9604 correctly reject a public Individual Vial request) — 12/12 passed. |

---

## How this list is maintained

- Every new owner-dependent blocker discovered during autonomous work gets added here at the time it's found, with the exact action, exact reason, and exact scope of what it blocks — never just "waiting on owner."
- Before adding a new entry, this list (plus `docs/PaymentReadiness.md`, `docs/Decisions.md`, `docs/ProductRoadmap.md`, and project memory) is checked first, so a question already answered here is never re-asked.
- An item's own linked document (`PaymentReadiness.md`, a specific `Decisions.md` entry) remains the authoritative detailed write-up; this table is the fast-scan index, not a replacement.
