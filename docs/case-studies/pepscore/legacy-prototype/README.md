# Pre-Next.js static prototype

`pre-nextjs-static-prototype.html` — a standalone static HTML mockup that predates the current Next.js App Router build. Moved here from the repo root 2026-08-17 (was sitting unreferenced and un-routed, with the obsolete "Holistic" branding the live app already dropped elsewhere). Not served by the app, not imported by any code, not referenced by any build/deploy config — confirmed by a full-repo search before moving. Kept as historical evidence of the site's earlier visual direction, not as production content.
